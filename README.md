# 6.2. Express 5: маршрутизация, валидация и ошибки

> Актуальная версия курса: Node.js 24 LTS, ECMAScript Modules и Express 5.

## О чём урок

Этот урок — самостоятельная глава курса. Материал построен от модели выполнения к проектным решениям: сначала разбираем, что делает платформа, затем пишем код, определяем отказоустойчивость и закрепляем результат практикой.

Старые примеры и устаревшие версии библиотек в эту редакцию не перенесены. Все объяснения сформулированы заново для современного Node.js.

## Как работать с материалом

1. Прочитайте раздел и сформулируйте его основную мысль своими словами.
2. До запуска примера предскажите результат и возможную ошибку.
3. Наберите код вручную, не вставляя его целиком.
4. Измените одно условие и объясните новое поведение.
5. Выполните мини-практику и ответьте на вопросы.
6. Сверьте нестабильный API с документацией Node.js 24.

## Требования

```bash
node --version
npm --version
```

Используйте Node.js 24.x. Новый код пишется в формате ESM. Встроенные модули импортируются с префиксом `node:`.

## 1. Продвинутая маршрутизация и обработка запросов в Express

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Опишите для темы «Продвинутая маршрутизация и обработка запросов в Express» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 2. Параметры маршрута, query параметры и тело запроса

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Соберите минимальный пример по теме «Параметры маршрута, query параметры и тело запроса» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 3. Введение в параметры маршрута

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: Async errors автоматически доходят до error middleware в Express 5

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Отделите чистое преобразование по теме «Введение в параметры маршрута» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 4. Объяснение, что такое параметры маршрута и как они используются для извлечения данных из URL.

HTTP-запрос состоит из метода, target, заголовков и необязательного тела; ответ — из статуса, заголовков и тела. Сервер должен завершить каждую ветку ровно одним ответом.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: Problem Details, 404, 409 и 422

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Опишите для темы «Объяснение, что такое параметры маршрута и как они используются для извлечения данных из URL.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 5. Примеры использования параметров маршрута в Express (app.get('/users/:userId', function(req, res) {...})).

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Соберите минимальный пример по теме «Примеры использования параметров маршрута в Express (app.get('/users/:userId', function(req, res) {...})).» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 6. Упражнения на извлечение параметров маршрута и их использование в логике обработки запросов

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Отделите чистое преобразование по теме «Упражнения на извлечение параметров маршрута и их использование в логике обработки запросов» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 7. Получение профиля пользователя

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Async errors автоматически доходят до error middleware в Express 5

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Опишите для темы «Получение профиля пользователя» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 8. Редактирование данных пользователя

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Problem Details, 404, 409 и 422

Неявная зависимость усложняет тестирование и приводит к различиям между локальным и production-окружением.

### Применение

Соберите минимальный пример по теме «Редактирование данных пользователя» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 9. Удаление пользователя

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Отделите чистое преобразование по теме «Удаление пользователя» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 10. Получение информации о книге по ISBN

Для прикладного асинхронного кода используется `node:fs/promises`. Синхронные функции допустимы в коротком CLI или на этапе запуска, но не в горячем пути HTTP-запроса.

FileHandle надо закрывать детерминированно. Высокоуровневые функции `readFile` и `writeFile` управляют handle сами, а низкоуровневый `open` передаёт ответственность вызывающему коду.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Параллельная запись в один файл без координации приводит к потерянным обновлениям.

### Применение

Опишите для темы «Получение информации о книге по ISBN» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 11. Query параметры

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: Async errors автоматически доходят до error middleware в Express 5

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Соберите минимальный пример по теме «Query параметры» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 12. Разбор концепции query параметров и их отличия от параметров маршрута.

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: Problem Details, 404, 409 и 422

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Отделите чистое преобразование по теме «Разбор концепции query параметров и их отличия от параметров маршрута.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 13. Примеры использования query параметров для фильтрации данных или передачи дополнительной информации в запросе (req.query).

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Опишите для темы «Примеры использования query параметров для фильтрации данных или передачи дополнительной информации в запросе (req.query).» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 14. Фильтрация списка книг по жанру и/или автору

Путь файловой системы и URL — разные модели. Путь строят через `node:path`, а ресурс рядом с ESM-модулем находят относительно `import.meta.url`.

Метаданные `stat` могут измениться сразу после проверки. Для безопасности нельзя строить решение только на последовательности check-then-use.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Параллельная запись в один файл без координации приводит к потерянным обновлениям.

### Применение

Соберите минимальный пример по теме «Фильтрация списка книг по жанру и/или автору» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 15. Пагинация списка пользователей

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Async errors автоматически доходят до error middleware в Express 5

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Отделите чистое преобразование по теме «Пагинация списка пользователей» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 16. Сортировка списка товаров

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Problem Details, 404, 409 и 422

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Опишите для темы «Сортировка списка товаров» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 17. Фильтрация списка заказов по дате

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Соберите минимальный пример по теме «Фильтрация списка заказов по дате» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 18. Практическое задание на обработку query параметров, создания API, которое позволяет фильтровать список пользователей по возрасту, имени, работе и зарплате.

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

Keep-alive переиспользует TCP-соединение, но сервер всё равно должен ограничивать таймауты заголовков, запроса и простоя.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Отделите чистое преобразование по теме «Практическое задание на обработку query параметров, создания API, которое позволяет фильтровать список пользователей по возрасту, имени, работе и зарплате.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 19. Работа с телом запроса

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: Async errors автоматически доходят до error middleware в Express 5

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Опишите для темы «Работа с телом запроса» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 20. Обзор методов для чтения тела запроса (req.body) и необходимость использования middleware, таких как express.json() и express.urlencoded().

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: Problem Details, 404, 409 и 422

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Соберите минимальный пример по теме «Обзор методов для чтения тела запроса (req.body) и необходимость использования middleware, таких как express.json() и express.urlencoded().» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 21. Демонстрация, как получать и использовать данные, отправленные в формате JSON или из форм.

Соединение аутентифицируется при handshake, а каждое действие авторизуется по текущему состоянию пользователя и ресурса.

Heartbeat обнаруживает полумёртвые соединения. Сервер отправляет ping, отмечает pong и завершает клиента, который не ответил в ожидаемом окне.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Доверие к userId из payload позволяет действовать от имени другого пользователя; идентичность берут из проверенного контекста соединения.

### Применение

Отделите чистое преобразование по теме «Демонстрация, как получать и использовать данные, отправленные в формате JSON или из форм.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 22. Задача на разработку функционала, требующего чтение и обработку тела POST-запроса, добавление нового пользователя, удаление, вывод и изменение данных.

Для прикладного асинхронного кода используется `node:fs/promises`. Синхронные функции допустимы в коротком CLI или на этапе запуска, но не в горячем пути HTTP-запроса.

Метаданные `stat` могут измениться сразу после проверки. Для безопасности нельзя строить решение только на последовательности check-then-use.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Наблюдение за каталогом зависит от ОС и не является гарантированной очередью бизнес-событий.

### Применение

Опишите для темы «Задача на разработку функционала, требующего чтение и обработку тела POST-запроса, добавление нового пользователя, удаление, вывод и изменение данных.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 23. Добавление нового пользователя:

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Async errors автоматически доходят до error middleware в Express 5

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Соберите минимальный пример по теме «Добавление нового пользователя:» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 24. Удаление пользователя:

Современное решение должно быть проверяемым, наблюдаемым и безопасным по умолчанию. Удобство демонстрации не должно скрывать production-ограничения.

Ресурс открывается как можно позже и закрывается детерминированно. Таймаут и отмена проходят через все поддерживающие их слои.

В контексте этого урока раздел связан с задачей: Problem Details, 404, 409 и 422

Обработка только happy path оставляет приложение без определённого поведения при сбое.

### Применение

Отделите чистое преобразование по теме «Удаление пользователя:» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 25. Получение списка всех пользователей:

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Опишите для темы «Получение списка всех пользователей:» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 26. Получение информации о конкретном пользователе:

Путь файловой системы и URL — разные модели. Путь строят через `node:path`, а ресурс рядом с ESM-модулем находят относительно `import.meta.url`.

Пользовательский относительный путь нормализуют, разрешают внутри доверенного корня и повторно проверяют итоговую границу.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Наблюдение за каталогом зависит от ОС и не является гарантированной очередью бизнес-событий.

### Применение

Соберите минимальный пример по теме «Получение информации о конкретном пользователе:» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 27. Обновление информации о пользователе:

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Async errors автоматически доходят до error middleware в Express 5

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Отделите чистое преобразование по теме «Обновление информации о пользователе:» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 28. Работа с middleware в Express

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: Problem Details, 404, 409 и 422

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Опишите для темы «Работа с middleware в Express» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 29. Введение в middleware

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Соберите минимальный пример по теме «Введение в middleware» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 30. Объяснение понятия middleware и его роли в обработке запросов в Express.

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Отделите чистое преобразование по теме «Объяснение понятия middleware и его роли в обработке запросов в Express.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 31. Обзор типов middleware: встроенные, сторонние и собственные.

HTTP-запрос состоит из метода, target, заголовков и необязательного тела; ответ — из статуса, заголовков и тела. Сервер должен завершить каждую ветку ровно одним ответом.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: Async errors автоматически доходят до error middleware в Express 5

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Опишите для темы «Обзор типов middleware: встроенные, сторонние и собственные.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 32. Примеры использования встроенных middleware (express.static, express.json()).

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Problem Details, 404, 409 и 422

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Соберите минимальный пример по теме «Примеры использования встроенных middleware (express.static, express.json()).» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 33. Пример использования express.static

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Отделите чистое преобразование по теме «Пример использования express.static» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 34. Пример использования express.json()

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Опишите для темы «Пример использования express.json()» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 35. Создание и использование собственных middleware

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

Keep-alive переиспользует TCP-соединение, но сервер всё равно должен ограничивать таймауты заголовков, запроса и простоя.

В контексте этого урока раздел связан с задачей: Async errors автоматически доходят до error middleware в Express 5

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Соберите минимальный пример по теме «Создание и использование собственных middleware» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 36. Пошаговое руководство по созданию собственного middleware. Объяснение структуры функции middleware и её параметров (req, res, next).

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Problem Details, 404, 409 и 422

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Отделите чистое преобразование по теме «Пошаговое руководство по созданию собственного middleware. Объяснение структуры функции middleware и её параметров (req, res, next).» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 37. Определение функции middleware

HTTP-запрос состоит из метода, target, заголовков и необязательного тела; ответ — из статуса, заголовков и тела. Сервер должен завершить каждую ветку ровно одним ответом.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Опишите для темы «Определение функции middleware» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 38. Параметры функции middleware

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Соберите минимальный пример по теме «Параметры функции middleware» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 39. Реализация логики middleware

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Async errors автоматически доходят до error middleware в Express 5

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Отделите чистое преобразование по теме «Реализация логики middleware» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 40. Пример создания собственного middleware

HTTP-запрос состоит из метода, target, заголовков и необязательного тела; ответ — из статуса, заголовков и тела. Сервер должен завершить каждую ветку ровно одним ответом.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: Problem Details, 404, 409 и 422

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Опишите для темы «Пример создания собственного middleware» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 41. Пример приложения

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Соберите минимальный пример по теме «Пример приложения» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 42. Настройка проекта

Запись важного файла проектируют с учётом сбоя посередине: временный файл, проверка результата и атомарный rename уменьшают вероятность повреждения.

Метаданные `stat` могут измениться сразу после проверки. Для безопасности нельзя строить решение только на последовательности check-then-use.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Параллельная запись в один файл без координации приводит к потерянным обновлениям.

### Применение

Отделите чистое преобразование по теме «Настройка проекта» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 43. Реализация маршрутов

HTTP-запрос состоит из метода, target, заголовков и необязательного тела; ответ — из статуса, заголовков и тела. Сервер должен завершить каждую ветку ровно одним ответом.

Keep-alive переиспользует TCP-соединение, но сервер всё равно должен ограничивать таймауты заголовков, запроса и простоя.

В контексте этого урока раздел связан с задачей: Async errors автоматически доходят до error middleware в Express 5

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Опишите для темы «Реализация маршрутов» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 44. Добавление новой книги

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Problem Details, 404, 409 и 422

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Соберите минимальный пример по теме «Добавление новой книги» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 45. Получение списка книг

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Отделите чистое преобразование по теме «Получение списка книг» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 46. Фильтрация книг по автору или жанру

Для прикладного асинхронного кода используется `node:fs/promises`. Синхронные функции допустимы в коротком CLI или на этапе запуска, но не в горячем пути HTTP-запроса.

FileHandle надо закрывать детерминированно. Высокоуровневые функции `readFile` и `writeFile` управляют handle сами, а низкоуровневый `open` передаёт ответственность вызывающему коду.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Наблюдение за каталогом зависит от ОС и не является гарантированной очередью бизнес-событий.

### Применение

Опишите для темы «Фильтрация книг по автору или жанру» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 47. Обновление информации о книге

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: Async errors автоматически доходят до error middleware в Express 5

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Соберите минимальный пример по теме «Обновление информации о книге» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 48. Работа с middleware

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: Problem Details, 404, 409 и 422

Два вызова отправки ответа приводят к `headers already sent`; обычно это ошибка управления потоком.

### Применение

Отделите чистое преобразование по теме «Работа с middleware» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 49. Логирование действий

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Router и разделение по ресурсу

Логирование всего тела запроса увеличивает стоимость, шум и риск утечки чувствительных данных.

### Применение

Опишите для темы «Логирование действий» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 50. Проверка наличия всех необходимых данных при добавлении книги

Путь файловой системы и URL — разные модели. Путь строят через `node:path`, а ресурс рядом с ESM-модулем находят относительно `import.meta.url`.

Метаданные `stat` могут измениться сразу после проверки. Для безопасности нельзя строить решение только на последовательности check-then-use.

В контексте этого урока раздел связан с задачей: Path params, query и body — разные источники

Наблюдение за каталогом зависит от ОС и не является гарантированной очередью бизнес-событий.

### Применение

Соберите минимальный пример по теме «Проверка наличия всех необходимых данных при добавлении книги» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## Цельный пример урока

Ниже приведён актуальный минимальный пример. Он размещён прямо в материале, чтобы теория и код не расходились по отдельным каталогам.

### `app.mjs`

```js
import express from 'express';
const tasks = new Map();
const router = express.Router();

router.post('/', (req, res) => {
  const title = req.body?.title?.trim();
  if (!title) return res.status(422).json({ title: 'Validation failed', status: 422 });
  const task = { id: crypto.randomUUID(), title, completed: false };
  tasks.set(task.id, task);
  res.status(201).location(`/tasks/${task.id}`).json(task);
});
router.get('/:id', async (req, res) => {
  const task = tasks.get(req.params.id);
  if (!task) {
    const error = new Error('Task not found');
    error.status = 404;
    throw error; // Express 5 передаст rejected Promise в error middleware.
  }
  res.json(task);
});

export const app = express();
app.use(express.json({ limit: '32kb' }));
app.use('/tasks', router);
app.use((req, res) => res.status(404).json({ title: 'Not Found', status: 404 }));
app.use((error, req, res, next) => {
  if (res.headersSent) return next(error);
  res.status(error.status ?? 500).json({ title: error.message, status: error.status ?? 500 });
});
```

### `app.test.mjs`

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { once } from 'node:events';
import { app } from './app.mjs';
test('creates and reads a task', async (t) => {
  const server = app.listen(0);
  t.after(() => server.close());
  await once(server, 'listening');
  const base = `http://127.0.0.1:${server.address().port}`;
  const createdResponse = await fetch(`${base}/tasks`, {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify({ title: 'Изучить Router' }),
  });
  assert.equal(createdResponse.status, 201);
  const created = await createdResponse.json();
  const found = await (await fetch(`${base}/tasks/${created.id}`)).json();
  assert.deepEqual(found, created);
});
```

### `package.json`

```json
{
  "name": "lesson-6-modern-example",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node app.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
```

### `server.mjs`

```js
import { app } from './app.mjs';
app.listen(3000, (error) => {
  if (error) throw error;
  console.log('API: http://localhost:3000');
});
```

## Итоговая работа

Соберите небольшой проект по теме урока. README проекта должен содержать назначение, требования, команды запуска, контракт входа и выхода, перечень ожидаемых ошибок и описание решения для graceful shutdown.

Минимальная проверка:

- успешный сценарий;
- неверный ввод;
- отказ внешней зависимости;
- timeout или отмена;
- повтор операции;
- завершение процесса;
- отсутствие секретов в логах и репозитории.

## Официальные источники

- [Routing](https://expressjs.com/en/5x/guide/routing/)
- [Error handling](https://expressjs.com/en/5x/guide/error-handling/)
- [Документация Node.js 24](https://nodejs.org/docs/latest-v24.x/api/)

- [Поддерживаемые версии Node.js](https://nodejs.org/en/about/previous-releases)
- [Node.js Learn](https://nodejs.org/en/learn)

## Критерии завершения урока

- Вы объясняете механизм без привязки к одному примеру.
- Вы понимаете границы встроенного API и внешней библиотеки.
- Вы умеете обработать ошибку, timeout и отмену.
- Вы можете проверить решение автоматическим тестом.
- Вы знаете, где найти статус и документацию использованного API.
