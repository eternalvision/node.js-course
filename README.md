# 6.4. Сессии, cookies, пароли и токены

> Актуальная версия курса: Node.js 24 LTS, ECMAScript Modules и Express 5.

## О чём урок

Этот урок — самостоятельная глава курса. Материал построен от модели выполнения к проектным решениям: сначала разбираем, что делает платформа, затем пишем код, определяем отказоустойчивость и закрепляем результат практикой.

Старые примеры и устаревшие версии библиотек в эту редакцию не перенесены. Все объяснения сформулированы заново для современного Node.js.

## Как работать с материалом

1. Прочитайте раздел и сформулируйте его основную мысль своими словами.
2. До запуска примера предскажите результат и возможную ошибку.
3. Наберите код вручную, не вставляя его целиком.
4. Измените одно условие и объясните новое поведение.
5. Выполните мини-практику и ответьте на вопросы.
6. Сверьте нестабильный API с документацией Node.js 24.

## Требования

```bash
node --version
npm --version
```

Используйте Node.js 24.x. Новый код пишется в формате ESM. Встроенные модули импортируются с префиксом `node:`.

## 1. Управление сессиями и аутентификация

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Опишите для темы «Управление сессиями и аутентификация» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 2. Основы управления сессиями в Express

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Соберите минимальный пример по теме «Основы управления сессиями в Express» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 3. Введение в сессии

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Отделите чистое преобразование по теме «Введение в сессии» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 4. Что такое сессия и как она используется для поддержания состояния между запросами.

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Опишите для темы «Что такое сессия и как она используется для поддержания состояния между запросами.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 5. Как сессии поддерживают состояние между запросами?

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Соберите минимальный пример по теме «Как сессии поддерживают состояние между запросами?» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 6. Различие между состоянием сессии и безсостоянием HTTP

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Отделите чистое преобразование по теме «Различие между состоянием сессии и безсостоянием HTTP» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 7. Настройка сессий в Express

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Опишите для темы «Настройка сессий в Express» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 8. Установка express-session

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Соберите минимальный пример по теме «Установка express-session» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 9. Настройка сессий в приложении Express

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Отделите чистое преобразование по теме «Настройка сессий в приложении Express» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 10. Демонстрация создания сессии и доступа к ней

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Опишите для темы «Демонстрация создания сессии и доступа к ней» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 11. Более сложный пример использования сессий в Express, включая аутентификацию пользователя и сохранение определённой информации о нём в сессии.

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Соберите минимальный пример по теме «Более сложный пример использования сессий в Express, включая аутентификацию пользователя и сохранение определённой информации о нём в сессии.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 12. Шаг 1: Установка необходимых пакетов

Конфигурация приходит из окружения, но строки из `process.env` всё равно требуют преобразования и проверки. Ошибка обязательной настройки должна останавливать приложение при старте.

dependencies нужны приложению во время работы, devDependencies — для разработки и сборки, peerDependencies описывают совместимость с зависимостью потребителя.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Слишком широкий диапазон версий без тестов может незаметно изменить транзитивную зависимость при следующей установке.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Установка необходимых пакетов» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 13. Шаг 2: Настройка базового Express приложения

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Опишите для темы «Шаг 2: Настройка базового Express приложения» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 14. Шаг 3: Реализация регистрации и аутентификации

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Соберите минимальный пример по теме «Шаг 3: Реализация регистрации и аутентификации» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 15. Хранение сессий

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Отделите чистое преобразование по теме «Хранение сессий» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 16. Хранение в памяти

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Опишите для темы «Хранение в памяти» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 17. Redis

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Соберите минимальный пример по теме «Redis» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 18. MongoDB

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Отделите чистое преобразование по теме «MongoDB» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 19. Пример реализации хранения сессий в MongoDB с использованием express, express-session и mongoose, выполните следующие шаги.

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Опишите для темы «Пример реализации хранения сессий в MongoDB с использованием express, express-session и mongoose, выполните следующие шаги.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 20. Шаг 2: Настройка Express и подключение к MongoDB

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Соберите минимальный пример по теме «Шаг 2: Настройка Express и подключение к MongoDB» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 21. Шаг 3: Настройка хранения сессий в MongoDB

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Отделите чистое преобразование по теме «Шаг 3: Настройка хранения сессий в MongoDB» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 22. Шаг 4: Создание маршрутов для демонстрации работы с сессиями

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Опишите для темы «Шаг 4: Создание маршрутов для демонстрации работы с сессиями» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 23. Безопасность сессий

Helmet устанавливает набор защитных заголовков, CORS управляет чтением ответа браузерным origin, rate limiter ограничивает частоту. Эти механизмы не заменяют authentication и authorization.

TLS обычно завершается на reverse proxy или load balancer. Приложение доверяет forwarded-заголовкам только от явно настроенных прокси.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Уязвимая или заброшенная зависимость остаётся частью attack surface даже при правильном собственном коде.

### Применение

Соберите минимальный пример по теме «Безопасность сессий» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 24. Использование HTTPS

Валидация подтверждает форму данных, параметризация защищает запрос, escaping защищает контекст вывода. Один слой не заменяет остальные.

TLS обычно завершается на reverse proxy или load balancer. Приложение доверяет forwarded-заголовкам только от явно настроенных прокси.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Уязвимая или заброшенная зависимость остаётся частью attack surface даже при правильном собственном коде.

### Применение

Отделите чистое преобразование по теме «Использование HTTPS» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 25. Настройка cookie

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Опишите для темы «Настройка cookie» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 26. Реализация ограничений на стороне клиента и сервера

Клиент внешнего API инкапсулирует base URL, авторизацию, таймаут, декодирование ответа и преобразование upstream-ошибок в собственный контракт.

Dependency injection функции fetch делает клиент тестируемым без реального интернета.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Неограниченное чтение ответа доверяет внешнему сервису контролировать память вашего процесса.

### Применение

Соберите минимальный пример по теме «Реализация ограничений на стороне клиента и сервера» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 27. Регулярное обновление и патчинг

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Отделите чистое преобразование по теме «Регулярное обновление и патчинг» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 28. Реальный пример веб-приложения на Express, которое реализует безопасность сессий с использованием HTTPS, настройки безопасных кук, а также CSRF-защиты. Этот пример включает в себя:

Безопасность начинается с модели угроз: какой актив защищается, кто атакует, через какую границу и с каким возможным ущербом. Middleware выбирают после ответа на эти вопросы.

CSRF актуален, когда браузер автоматически прикладывает cookie. Защита сочетает SameSite, проверку Origin/Referer и token для изменяющих запросов.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Разрешение CORS `*` вместе с неверной моделью credentials либо не работает, либо открывает лишний доступ.

### Применение

Опишите для темы «Реальный пример веб-приложения на Express, которое реализует безопасность сессий с использованием HTTPS, настройки безопасных кук, а также CSRF-защиты. Этот пример включает в себя:» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 29. Шаг 1: Принудительное использование HTTPS

Helmet устанавливает набор защитных заголовков, CORS управляет чтением ответа браузерным origin, rate limiter ограничивает частоту. Эти механизмы не заменяют authentication и authorization.

CSRF актуален, когда браузер автоматически прикладывает cookie. Защита сочетает SameSite, проверку Origin/Referer и token для изменяющих запросов.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Разрешение CORS `*` вместе с неверной моделью credentials либо не работает, либо открывает лишний доступ.

### Применение

Соберите минимальный пример по теме «Шаг 1: Принудительное использование HTTPS» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 30. Обратите внимание:

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Отделите чистое преобразование по теме «Обратите внимание:» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 31. Реализация аутентификации с использованием JSON Web Token (JWT), сессий и cookies

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Опишите для темы «Реализация аутентификации с использованием JSON Web Token (JWT), сессий и cookies» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 32. Введение в JWT

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Соберите минимальный пример по теме «Введение в JWT» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 33. Что такое JWT и как он работает.

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Отделите чистое преобразование по теме «Что такое JWT и как он работает.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 34. Кодирование и декодирование JWT

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Опишите для темы «Кодирование и декодирование JWT» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 35. Преимущества использования JWT для аутентификации.

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Соберите минимальный пример по теме «Преимущества использования JWT для аутентификации.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 36. Создание и верификация JWT

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Отделите чистое преобразование по теме «Создание и верификация JWT» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 37. Реализация генерации JWT при успешной аутентификации пользователя.

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Опишите для темы «Реализация генерации JWT при успешной аутентификации пользователя.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 38. Стратегия JWT

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Соберите минимальный пример по теме «Стратегия JWT» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 39. Роут регистрации

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Отделите чистое преобразование по теме «Роут регистрации» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 40. Получение токена для авторизации

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Опишите для темы «Получение токена для авторизации» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 41. Закрытый маршрут

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Соберите минимальный пример по теме «Закрытый маршрут» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 42. Давайте структурируем приложение

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Отделите чистое преобразование по теме «Давайте структурируем приложение» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 43. Структура проекта

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Опишите для темы «Структура проекта» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 44. index.js

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Соберите минимальный пример по теме «index.js» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 45. config/passport.js

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Отделите чистое преобразование по теме «config/passport.js» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 46. models/user.js

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Опишите для темы «models/user.js» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 47. routes/users.js

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Соберите минимальный пример по теме «routes/users.js» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 48. middlewares/auth.js

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Отделите чистое преобразование по теме «middlewares/auth.js» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 49. Настройки порта

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Опишите для темы «Настройки порта» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 50. URI подключения к MongoDB

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Уникальный индекс и обработка duplicate key необходимы даже при предварительной проверке существования.

### Применение

Соберите минимальный пример по теме «URI подключения к MongoDB» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 51. Секретный ключ для подписи JWT

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Отделите чистое преобразование по теме «Секретный ключ для подписи JWT» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 52. Использование сессий и cookies для аутентификации

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Опишите для темы «Использование сессий и cookies для аутентификации» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 53. Сохранение JWT в куки

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Соберите минимальный пример по теме «Сохранение JWT в куки» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 54. Настройка безопасности куки

Валидация подтверждает форму данных, параметризация защищает запрос, escaping защищает контекст вывода. Один слой не заменяет остальные.

Rate limit выбирается по идентичности и стоимости операции; общий IP-лимит за NAT может блокировать множество честных пользователей.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Уязвимая или заброшенная зависимость остаётся частью attack surface даже при правильном собственном коде.

### Применение

Отделите чистое преобразование по теме «Настройка безопасности куки» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 55. Использование куки для аутентификации в последующих запросах

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Опишите для темы «Использование куки для аутентификации в последующих запросах» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 56. Небольшое Express-приложение, которое демонстрирует аутентификацию с использованием JWT и сохранение токена в куки.

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Соберите минимальный пример по теме «Небольшое Express-приложение, которое демонстрирует аутентификацию с использованием JWT и сохранение токена в куки.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 57. Шаг 1: Установка зависимостей

Конфигурация приходит из окружения, но строки из `process.env` всё равно требуют преобразования и проверки. Ошибка обязательной настройки должна останавливать приложение при старте.

dependencies нужны приложению во время работы, devDependencies — для разработки и сборки, peerDependencies описывают совместимость с зависимостью потребителя.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Секрет в репозитории остаётся скомпрометированным даже после удаления из последнего коммита; его необходимо отозвать и заменить.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Установка зависимостей» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 58. Шаг 2: Создание файла .env

Для прикладного асинхронного кода используется `node:fs/promises`. Синхронные функции допустимы в коротком CLI или на этапе запуска, но не в горячем пути HTTP-запроса.

Метаданные `stat` могут измениться сразу после проверки. Для безопасности нельзя строить решение только на последовательности check-then-use.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Path traversal позволяет выйти из разрешённого каталога через `..`, абсолютный путь или особенности платформы.

### Применение

Опишите для темы «Шаг 2: Создание файла .env» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 59. Шаг 3: Создание базового сервера

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Соберите минимальный пример по теме «Шаг 3: Создание базового сервера» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 60. Обновление и отзыв JWT

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Отделите чистое преобразование по теме «Обновление и отзыв JWT» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 61. Механизмы обновления токенов доступа через refresh токены.

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Опишите для темы «Механизмы обновления токенов доступа через refresh токены.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 62. Шаг 1: Генерация Access и Refresh Токенов

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Соберите минимальный пример по теме «Шаг 1: Генерация Access и Refresh Токенов» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 63. Шаг 2: Создание Эндпоинта для Обновления Токена

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Отделите чистое преобразование по теме «Шаг 2: Создание Эндпоинта для Обновления Токена» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 64. Шаг 3: Отзыв Refresh Токенов

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Опишите для темы «Шаг 3: Отзыв Refresh Токенов» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 65. Хранение Refresh Токенов

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Соберите минимальный пример по теме «Хранение Refresh Токенов» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 66. Реализация Секретных Ключей

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Отделите чистое преобразование по теме «Реализация Секретных Ключей» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 67. Механизмы обновления и отзыва JWT, используя Node.js и Express. Простуая система аутентификации с двумя основными маршрутами: один для входа (генерация access и refresh токенов) и один для обновления access токена, используя refresh токен.

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Опишите для темы «Механизмы обновления и отзыва JWT, используя Node.js и Express. Простуая система аутентификации с двумя основными маршрутами: один для входа (генерация access и refresh токенов) и один для обновления access токена, используя refresh токен.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 68. Шаг 1: Подготовка и начальная настройка

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Соберите минимальный пример по теме «Шаг 1: Подготовка и начальная настройка» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 69. Шаг 2: Создание сервера Express

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Отделите чистое преобразование по теме «Шаг 2: Создание сервера Express» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 70. Объяснение:

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Опишите для темы «Объяснение:» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 71. Шаг 3: Тестирование

Встроенный `node:test` поддерживает suites, hooks, mocking, watch mode и coverage. Для большинства учебных примеров отдельный test framework не обязателен.

Diagnostic report, inspector, CPU profile и heap snapshot отвечают на разные вопросы; инструмент выбирают после формулировки симптома.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Чрезмерный mock проверяет собственные ожидания теста, а не совместимость с реальной системой.

### Применение

Соберите минимальный пример по теме «Шаг 3: Тестирование» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 72. Практические примеры аутентификации без использования сторонних сервисов

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Отделите чистое преобразование по теме «Практические примеры аутентификации без использования сторонних сервисов» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 73. Аутентификация с использованием Passport.js

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Опишите для темы «Аутентификация с использованием Passport.js» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 74. Реализация локальной стратегии аутентификации с использованием passport-local.

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Соберите минимальный пример по теме «Реализация локальной стратегии аутентификации с использованием passport-local.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 75. Шаг 2: Настройка Express и Passport

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Отделите чистое преобразование по теме «Шаг 2: Настройка Express и Passport» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 76. Шаг 3: Тестирование функциональности

Тест проверяет наблюдаемое поведение и контракт ошибки, а не случайную внутреннюю последовательность вызовов. Это позволяет менять реализацию без переписывания всей suite.

Dependency injection часов, random и fetch делает тест детерминированным без глобальной подмены runtime.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Snapshot большого объекта скрывает значимое изменение среди шума; критичные поля лучше утверждать явно.

### Применение

Опишите для темы «Шаг 3: Тестирование функциональности» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 77. Хеширование и проверка паролей с использованием bcrypt.

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Соберите минимальный пример по теме «Хеширование и проверка паролей с использованием bcrypt.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 78. Шаг 1: Установка bcryptjs

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Установка bcryptjs» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 79. Шаг 2: Хеширование пароля

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Опишите для темы «Шаг 2: Хеширование пароля» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 80. Шаг 3: Проверка пароля

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Соберите минимальный пример по теме «Шаг 3: Проверка пароля» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 81. Пример использования

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

### Применение

Отделите чистое преобразование по теме «Пример использования» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 82. Реализация аутентификации с Handlebars на основе сессий

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Опишите для темы «Реализация аутентификации с Handlebars на основе сессий» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 83. Шаг 1: Настройка проекта

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Соберите минимальный пример по теме «Шаг 1: Настройка проекта» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 84. Шаг 2: Настройка Express и Handlebars

Layout задаёт общий каркас, partial переиспользует небольшой фрагмент, helper инкапсулирует простое отображение. Бизнес-логику в шаблон не переносят.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Отделите чистое преобразование по теме «Шаг 2: Настройка Express и Handlebars» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 85. Шаг 3: Создание форм входа и регистрации

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Authentication против authorization

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Опишите для темы «Шаг 3: Создание форм входа и регистрации» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 86. Шаг 4: Реализация регистрации и входа

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: HttpOnly, Secure и SameSite cookies

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Соберите минимальный пример по теме «Шаг 4: Реализация регистрации и входа» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 87. Шаг 5: Управление состоянием аутентификации через сессии

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

В контексте этого урока раздел связан с задачей: Сессия хранится сервером, cookie — только идентификатор

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

### Применение

Отделите чистое преобразование по теме «Шаг 5: Управление состоянием аутентификации через сессии» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 88. Шаг 6: Запуск сервера

HTTP-запрос состоит из метода, target, заголовков и необязательного тела; ответ — из статуса, заголовков и тела. Сервер должен завершить каждую ветку ровно одним ответом.

Keep-alive переиспользует TCP-соединение, но сервер всё равно должен ограничивать таймауты заголовков, запроса и простоя.

В контексте этого урока раздел связан с задачей: Password hashing, rotation и короткоживущие access tokens

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Опишите для темы «Шаг 6: Запуск сервера» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## Цельный пример урока

Ниже приведён актуальный минимальный пример. Он размещён прямо в материале, чтобы теория и код не расходились по отдельным каталогам.

### `app.mjs`

```js
import express from 'express';
import session from 'express-session';
const app = express();
app.set('trust proxy', 1);
app.use(express.json());
app.use(session({
  name: 'sid',
  secret: process.env.SESSION_SECRET,
  resave: false,
  saveUninitialized: false,
  cookie: { httpOnly: true, secure: 'auto', sameSite: 'lax', maxAge: 3_600_000 },
}));
app.post('/login', (req, res) => {
  // В реальном приложении сравните password hash и regenerate session.
  req.session.userId = 'demo-user';
  res.sendStatus(204);
});
app.get('/me', (req, res) =>
  req.session.userId ? res.json({ id: req.session.userId }) : res.sendStatus(401));
app.post('/logout', (req, res, next) => req.session.destroy((error) => error ? next(error) : res.sendStatus(204)));
app.listen(3000);
```

### `package.json`

```json
{
  "name": "lesson-6-modern-example",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node app.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0",
    "express-session": "^1.18.2"
  }
}
```

## Дополнительный разбор 1: Управление сессиями и аутентификация

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

Цель: Authentication против authorization

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 2: Основы управления сессиями в Express

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

Цель: HttpOnly, Secure и SameSite cookies

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 3: Введение в сессии

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

Цель: Сессия хранится сервером, cookie — только идентификатор

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 4: Что такое сессия и как она используется для поддержания состояния между запросами.

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

Цель: Password hashing, rotation и короткоживущие access tokens

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 5: Как сессии поддерживают состояние между запросами?

Серверная сессия хранит состояние на backend, а cookie — случайный идентификатор. JWT является подписанным форматом claims, но не механизмом шифрования и не готовой системой отзыва.

Session cookie получает HttpOnly, Secure и подходящий SameSite. После login идентификатор регенерируют, после logout серверную сессию уничтожают.

Цель: Authentication против authorization

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 6: Различие между состоянием сессии и безсостоянием HTTP

Authentication устанавливает личность, authorization отвечает, разрешено ли конкретное действие над конкретным объектом. Успешный login не даёт автоматического доступа ко всем ресурсам.

Access token делают короткоживущим, проверяют algorithm, issuer, audience и expiration. Refresh token вращают и отслеживают повторное использование.

Цель: HttpOnly, Secure и SameSite cookies

Долгоживущий JWT без механизма отзыва продолжает работать после logout, смены роли или компрометации.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 7: Настройка сессий в Express

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

Цель: Сессия хранится сервером, cookie — только идентификатор

Хранение bearer token в доступном JavaScript хранилище увеличивает последствия XSS.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Итоговая работа

Соберите небольшой проект по теме урока. README проекта должен содержать назначение, требования, команды запуска, контракт входа и выхода, перечень ожидаемых ошибок и описание решения для graceful shutdown.

Минимальная проверка:

- успешный сценарий;
- неверный ввод;
- отказ внешней зависимости;
- timeout или отмена;
- повтор операции;
- завершение процесса;
- отсутствие секретов в логах и репозитории.

## Официальные источники

- [Express security](https://expressjs.com/en/advanced/best-practice-security.html)
- [OWASP Authentication](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)
- [Документация Node.js 24](https://nodejs.org/docs/latest-v24.x/api/)

- [Поддерживаемые версии Node.js](https://nodejs.org/en/about/previous-releases)
- [Node.js Learn](https://nodejs.org/en/learn)

## Критерии завершения урока

- Вы объясняете механизм без привязки к одному примеру.
- Вы понимаете границы встроенного API и внешней библиотеки.
- Вы умеете обработать ошибку, timeout и отмену.
- Вы можете проверить решение автоматическим тестом.
- Вы знаете, где найти статус и документацию использованного API.
