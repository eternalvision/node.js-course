# 8. Транзакционная почта: Nodemailer, провайдер и очередь

> Актуальная версия курса: Node.js 24 LTS, ECMAScript Modules и Express 5.

## О чём урок

Этот урок — самостоятельная глава курса. Материал построен от модели выполнения к проектным решениям: сначала разбираем, что делает платформа, затем пишем код, определяем отказоустойчивость и закрепляем результат практикой.

Старые примеры и устаревшие версии библиотек в эту редакцию не перенесены. Все объяснения сформулированы заново для современного Node.js.

## Как работать с материалом

1. Прочитайте раздел и сформулируйте его основную мысль своими словами.
2. До запуска примера предскажите результат и возможную ошибку.
3. Наберите код вручную, не вставляя его целиком.
4. Измените одно условие и объясните новое поведение.
5. Выполните мини-практику и ответьте на вопросы.
6. Сверьте нестабильный API с документацией Node.js 24.

## Требования

```bash
node --version
npm --version
```

Используйте Node.js 24.x. Новый код пишется в формате ESM. Встроенные модули импортируются с префиксом `node:`.

## 1. Почта. SendGrid. Nodemailer

Отправка письма — внешняя побочная операция. HTTP-запрос обычно фиксирует намерение в очереди, а worker выполняет доставку с retry и наблюдаемым статусом.

Nodemailer создаёт transporter один раз и переиспользует соединения там, где это поддерживает провайдер.

В контексте этого урока раздел связан с задачей: SMTP и API-провайдеры

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

### Применение

Опишите для темы «Почта. SendGrid. Nodemailer» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 2. Введение и теоретические основы

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: Шаблоны и text fallback

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Соберите минимальный пример по теме «Введение и теоретические основы» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 3. Введение в тему

Retry разрешён только при понятной идемпотентности. Между попытками применяют ограничение количества, exponential backoff, jitter и общий deadline.

AbortSignal передаётся в fetch и связывает отмену вызывающего кода с сетевой операцией. Таймаут является одной из причин отмены, а не отдельным способом обработки ошибок.

В контексте этого урока раздел связан с задачей: Очередь, retry и idempotency

Автоматический retry POST без idempotency key способен создать дубликаты платежа, письма или записи.

### Применение

Отделите чистое преобразование по теме «Введение в тему» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 4. Объяснение важности работы с электронной почтой в современных веб-приложениях

Отправка письма — внешняя побочная операция. HTTP-запрос обычно фиксирует намерение в очереди, а worker выполняет доставку с retry и наблюдаемым статусом.

Idempotency key не даёт повторной доставке job отправить одинаковое транзакционное письмо несколько раз.

В контексте этого урока раздел связан с задачей: SPF, DKIM, DMARC, bounce и complaint

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

### Применение

Опишите для темы «Объяснение важности работы с электронной почтой в современных веб-приложениях» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 5. Краткий обзор инструментов для работы с почтой в Node.js: SendGrid и Nodemailer

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Idempotency key не даёт повторной доставке job отправить одинаковое транзакционное письмо несколько раз.

В контексте этого урока раздел связан с задачей: SMTP и API-провайдеры

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

### Применение

Соберите минимальный пример по теме «Краткий обзор инструментов для работы с почтой в Node.js: SendGrid и Nodemailer» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 6. Обзор SendGrid

SPF, DKIM и DMARC подтверждают разрешённые источники и политику домена. Bounce и complaint должны менять дальнейшую стратегию отправки.

Idempotency key не даёт повторной доставке job отправить одинаковое транзакционное письмо несколько раз.

В контексте этого урока раздел связан с задачей: Шаблоны и text fallback

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

### Применение

Отделите чистое преобразование по теме «Обзор SendGrid» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 7. Что такое SendGrid и какие преимущества он предлагает

Отправка письма — внешняя побочная операция. HTTP-запрос обычно фиксирует намерение в очереди, а worker выполняет доставку с retry и наблюдаемым статусом.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

В контексте этого урока раздел связан с задачей: Очередь, retry и idempotency

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

### Применение

Опишите для темы «Что такое SendGrid и какие преимущества он предлагает» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 8. Основные возможности и типы отправляемых сообщений через SendGrid

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

В контексте этого урока раздел связан с задачей: SPF, DKIM, DMARC, bounce и complaint

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

### Применение

Соберите минимальный пример по теме «Основные возможности и типы отправляемых сообщений через SendGrid» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 9. Обзор Nodemailer

SPF, DKIM и DMARC подтверждают разрешённые источники и политику домена. Bounce и complaint должны менять дальнейшую стратегию отправки.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

В контексте этого урока раздел связан с задачей: SMTP и API-провайдеры

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

### Применение

Отделите чистое преобразование по теме «Обзор Nodemailer» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 10. Что такое Nodemailer и когда его стоит использовать

Отправка письма — внешняя побочная операция. HTTP-запрос обычно фиксирует намерение в очереди, а worker выполняет доставку с retry и наблюдаемым статусом.

Nodemailer создаёт transporter один раз и переиспользует соединения там, где это поддерживает провайдер.

В контексте этого урока раздел связан с задачей: Шаблоны и text fallback

Бесконечный retry постоянной ошибки создаёт шум и блокирует очередь; нужны классификация и dead-letter.

### Применение

Опишите для темы «Что такое Nodemailer и когда его стоит использовать» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 11. Основные возможности и типы отправляемых сообщений через Nodemailer

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Nodemailer создаёт transporter один раз и переиспользует соединения там, где это поддерживает провайдер.

В контексте этого урока раздел связан с задачей: Очередь, retry и idempotency

Бесконечный retry постоянной ошибки создаёт шум и блокирует очередь; нужны классификация и dead-letter.

### Применение

Соберите минимальный пример по теме «Основные возможности и типы отправляемых сообщений через Nodemailer» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 12. Типы отправляемых сообщений

Современное решение должно быть проверяемым, наблюдаемым и безопасным по умолчанию. Удобство демонстрации не должно скрывать production-ограничения.

Входные данные нормализуются на границе, бизнес-правила выполняются отдельно, а побочные эффекты получают явного владельца.

В контексте этого урока раздел связан с задачей: SPF, DKIM, DMARC, bounce и complaint

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Отделите чистое преобразование по теме «Типы отправляемых сообщений» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 13. Практическая работа с Nodemailer

Отправка письма — внешняя побочная операция. HTTP-запрос обычно фиксирует намерение в очереди, а worker выполняет доставку с retry и наблюдаемым статусом.

Idempotency key не даёт повторной доставке job отправить одинаковое транзакционное письмо несколько раз.

В контексте этого урока раздел связан с задачей: SMTP и API-провайдеры

Бесконечный retry постоянной ошибки создаёт шум и блокирует очередь; нужны классификация и dead-letter.

### Применение

Опишите для темы «Практическая работа с Nodemailer» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 14. Настройка проекта

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: Шаблоны и text fallback

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Соберите минимальный пример по теме «Настройка проекта» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 15. Создание нового проекта на Express

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Очередь, retry и idempotency

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Отделите чистое преобразование по теме «Создание нового проекта на Express» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 16. Настройка транспорта SMTP

Отправка письма — внешняя побочная операция. HTTP-запрос обычно фиксирует намерение в очереди, а worker выполняет доставку с retry и наблюдаемым статусом.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

В контексте этого урока раздел связан с задачей: SPF, DKIM, DMARC, bounce и complaint

Бесконечный retry постоянной ошибки создаёт шум и блокирует очередь; нужны классификация и dead-letter.

### Применение

Опишите для темы «Настройка транспорта SMTP» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 17. Создание простого маршрута для отправки почты

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

В контексте этого урока раздел связан с задачей: SMTP и API-провайдеры

Бесконечный retry постоянной ошибки создаёт шум и блокирует очередь; нужны классификация и dead-letter.

### Применение

Соберите минимальный пример по теме «Создание простого маршрута для отправки почты» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 18. Тестирование отправки письма

Coverage показывает непройденные строки и ветви, но не оценивает качество assertions. Сначала выбирают риск, затем сценарий.

Diagnostic report, inspector, CPU profile и heap snapshot отвечают на разные вопросы; инструмент выбирают после формулировки симптома.

В контексте этого урока раздел связан с задачей: Шаблоны и text fallback

Чрезмерный mock проверяет собственные ожидания теста, а не совместимость с реальной системой.

### Применение

Отделите чистое преобразование по теме «Тестирование отправки письма» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 19. Расширенные возможности Nodemailer

Отправка письма — внешняя побочная операция. HTTP-запрос обычно фиксирует намерение в очереди, а worker выполняет доставку с retry и наблюдаемым статусом.

Nodemailer создаёт transporter один раз и переиспользует соединения там, где это поддерживает провайдер.

В контексте этого урока раздел связан с задачей: Очередь, retry и idempotency

Письмо с непроверенным адресом отправителя ломает репутацию домена и может быть отклонено политиками DMARC.

### Применение

Опишите для темы «Расширенные возможности Nodemailer» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 20. Работа с шаблонами писем

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: SPF, DKIM, DMARC, bounce и complaint

Смешение API JSON и HTML-ответов в одном handler усложняет negotiation и обработку ошибок.

### Применение

Соберите минимальный пример по теме «Работа с шаблонами писем» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 21. Отправка вложений

SPF, DKIM и DMARC подтверждают разрешённые источники и политику домена. Bounce и complaint должны менять дальнейшую стратегию отправки.

Nodemailer создаёт transporter один раз и переиспользует соединения там, где это поддерживает провайдер.

В контексте этого урока раздел связан с задачей: SMTP и API-провайдеры

Письмо с непроверенным адресом отправителя ломает репутацию домена и может быть отклонено политиками DMARC.

### Применение

Отделите чистое преобразование по теме «Отправка вложений» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 22. Практическая работа с SendGrid

Отправка письма — внешняя побочная операция. HTTP-запрос обычно фиксирует намерение в очереди, а worker выполняет доставку с retry и наблюдаемым статусом.

Idempotency key не даёт повторной доставке job отправить одинаковое транзакционное письмо несколько раз.

В контексте этого урока раздел связан с задачей: Шаблоны и text fallback

Письмо с непроверенным адресом отправителя ломает репутацию домена и может быть отклонено политиками DMARC.

### Применение

Опишите для темы «Практическая работа с SendGrid» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 23. Настройка SendGrid

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Idempotency key не даёт повторной доставке job отправить одинаковое транзакционное письмо несколько раз.

В контексте этого урока раздел связан с задачей: Очередь, retry и idempotency

Письмо с непроверенным адресом отправителя ломает репутацию домена и может быть отклонено политиками DMARC.

### Применение

Соберите минимальный пример по теме «Настройка SendGrid» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 24. Регистрация и настройка аккаунта на SendGrid

SPF, DKIM и DMARC подтверждают разрешённые источники и политику домена. Bounce и complaint должны менять дальнейшую стратегию отправки.

Idempotency key не даёт повторной доставке job отправить одинаковое транзакционное письмо несколько раз.

В контексте этого урока раздел связан с задачей: SPF, DKIM, DMARC, bounce и complaint

Письмо с непроверенным адресом отправителя ломает репутацию домена и может быть отклонено политиками DMARC.

### Применение

Отделите чистое преобразование по теме «Регистрация и настройка аккаунта на SendGrid» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 25. Получение API ключа и настройка в проекте

HTTP-запрос состоит из метода, target, заголовков и необязательного тела; ответ — из статуса, заголовков и тела. Сервер должен завершить каждую ветку ровно одним ответом.

Keep-alive переиспользует TCP-соединение, но сервер всё равно должен ограничивать таймауты заголовков, запроса и простоя.

В контексте этого урока раздел связан с задачей: SMTP и API-провайдеры

Два вызова отправки ответа приводят к `headers already sent`; обычно это ошибка управления потоком.

### Применение

Опишите для темы «Получение API ключа и настройка в проекте» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 26. Реализация отправки почты через SendGrid

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

В контексте этого урока раздел связан с задачей: Шаблоны и text fallback

Письмо с непроверенным адресом отправителя ломает репутацию домена и может быть отклонено политиками DMARC.

### Применение

Соберите минимальный пример по теме «Реализация отправки почты через SendGrid» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 27. Использование официального клиента SendGrid для Node.js

SPF, DKIM и DMARC подтверждают разрешённые источники и политику домена. Bounce и complaint должны менять дальнейшую стратегию отправки.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

В контексте этого урока раздел связан с задачей: Очередь, retry и idempotency

Письмо с непроверенным адресом отправителя ломает репутацию домена и может быть отклонено политиками DMARC.

### Применение

Отделите чистое преобразование по теме «Использование официального клиента SendGrid для Node.js» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 28. Создание и отправка писем через API SendGrid

Отправка письма — внешняя побочная операция. HTTP-запрос обычно фиксирует намерение в очереди, а worker выполняет доставку с retry и наблюдаемым статусом.

Nodemailer создаёт transporter один раз и переиспользует соединения там, где это поддерживает провайдер.

В контексте этого урока раздел связан с задачей: SPF, DKIM, DMARC, bounce и complaint

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

### Применение

Опишите для темы «Создание и отправка писем через API SendGrid» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 29. Расширенное использование SendGrid

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Nodemailer создаёт transporter один раз и переиспользует соединения там, где это поддерживает провайдер.

В контексте этого урока раздел связан с задачей: SMTP и API-провайдеры

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

### Применение

Соберите минимальный пример по теме «Расширенное использование SendGrid» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 30. Массовая отправка писем

Layout задаёт общий каркас, partial переиспользует небольшой фрагмент, helper инкапсулирует простое отображение. Бизнес-логику в шаблон не переносят.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: Шаблоны и text fallback

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Отделите чистое преобразование по теме «Массовая отправка писем» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 31. Персонализация писем

Встроенный fetch является стандартным HTTP-клиентом для большинства интеграций. Он отклоняет Promise при сетевой ошибке, но ответы 4xx и 5xx надо проверять через `response.ok`.

Response body — поток и может быть прочитан один раз. Сначала проверяют статус и тип содержимого, затем выбирают `json`, `text`, `arrayBuffer` или streaming.

В контексте этого урока раздел связан с задачей: Очередь, retry и idempotency

Автоматический retry POST без idempotency key способен создать дубликаты платежа, письма или записи.

### Применение

Опишите для темы «Персонализация писем» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 32. Использование шаблонов

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: SPF, DKIM, DMARC, bounce и complaint

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Соберите минимальный пример по теме «Использование шаблонов» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 33. Дополнительные заголовки и параметры

SPF, DKIM и DMARC подтверждают разрешённые источники и политику домена. Bounce и complaint должны менять дальнейшую стратегию отправки.

Idempotency key не даёт повторной доставке job отправить одинаковое транзакционное письмо несколько раз.

В контексте этого урока раздел связан с задачей: SMTP и API-провайдеры

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

### Применение

Отделите чистое преобразование по теме «Дополнительные заголовки и параметры» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 34. Обработка ответов и ошибок

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Шаблоны и text fallback

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Опишите для темы «Обработка ответов и ошибок» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 35. Сравнение SendGrid с Nodemailer

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

В контексте этого урока раздел связан с задачей: Очередь, retry и idempotency

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

### Применение

Соберите минимальный пример по теме «Сравнение SendGrid с Nodemailer» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 36. Преимущества и недостатки

Современное решение должно быть проверяемым, наблюдаемым и безопасным по умолчанию. Удобство демонстрации не должно скрывать production-ограничения.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: SPF, DKIM, DMARC, bounce и complaint

Неявная зависимость усложняет тестирование и приводит к различиям между локальным и production-окружением.

### Применение

Отделите чистое преобразование по теме «Преимущества и недостатки» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 37. Выбор подходящего инструмента в зависимости от задачи

package.json описывает контракт запуска: формат модулей, scripts, поддерживаемую Node.js и прямые зависимости. Lock-файл фиксирует полный граф и делает установку воспроизводимой.

dependencies нужны приложению во время работы, devDependencies — для разработки и сборки, peerDependencies описывают совместимость с зависимостью потребителя.

В контексте этого урока раздел связан с задачей: SMTP и API-провайдеры

Слишком широкий диапазон версий без тестов может незаметно изменить транзитивную зависимость при следующей установке.

### Применение

Опишите для темы «Выбор подходящего инструмента в зависимости от задачи» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## Цельный пример урока

Ниже приведён актуальный минимальный пример. Он размещён прямо в материале, чтобы теория и код не расходились по отдельным каталогам.

### `mailer.mjs`

```js
import nodemailer from 'nodemailer';
export const createMailer = (transport = nodemailer.createTransport({
  host: process.env.SMTP_HOST,
  port: Number(process.env.SMTP_PORT ?? 587),
  secure: false,
  auth: { user: process.env.SMTP_USER, pass: process.env.SMTP_PASSWORD },
})) => ({
  sendWelcome({ to, name }) {
    return transport.sendMail({
      from: process.env.MAIL_FROM,
      to,
      subject: 'Добро пожаловать',
      text: `Здравствуйте, ${name}!`,
      html: `<p>Здравствуйте, <strong>${name}</strong>!</p>`,
    });
  },
});
```

### `package.json`

```json
{
  "name": "lesson-8-modern-example",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node mailer.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "nodemailer": "^7.0.0"
  }
}
```

## Дополнительный разбор 1: Почта. SendGrid. Nodemailer

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

Цель: SMTP и API-провайдеры

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 2: Введение и теоретические основы

Layout задаёт общий каркас, partial переиспользует небольшой фрагмент, helper инкапсулирует простое отображение. Бизнес-логику в шаблон не переносят.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

Цель: Шаблоны и text fallback

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 3: Введение в тему

Встроенный fetch является стандартным HTTP-клиентом для большинства интеграций. Он отклоняет Promise при сетевой ошибке, но ответы 4xx и 5xx надо проверять через `response.ok`.

Response body — поток и может быть прочитан один раз. Сначала проверяют статус и тип содержимого, затем выбирают `json`, `text`, `arrayBuffer` или streaming.

Цель: Очередь, retry и idempotency

Неограниченное чтение ответа доверяет внешнему сервису контролировать память вашего процесса.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 4: Объяснение важности работы с электронной почтой в современных веб-приложениях

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

Цель: SPF, DKIM, DMARC, bounce и complaint

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 5: Краткий обзор инструментов для работы с почтой в Node.js: SendGrid и Nodemailer

SPF, DKIM и DMARC подтверждают разрешённые источники и политику домена. Bounce и complaint должны менять дальнейшую стратегию отправки.

Nodemailer создаёт transporter один раз и переиспользует соединения там, где это поддерживает провайдер.

Цель: SMTP и API-провайдеры

Бесконечный retry постоянной ошибки создаёт шум и блокирует очередь; нужны классификация и dead-letter.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 6: Обзор SendGrid

Отправка письма — внешняя побочная операция. HTTP-запрос обычно фиксирует намерение в очереди, а worker выполняет доставку с retry и наблюдаемым статусом.

Idempotency key не даёт повторной доставке job отправить одинаковое транзакционное письмо несколько раз.

Цель: Шаблоны и text fallback

Письмо с непроверенным адресом отправителя ломает репутацию домена и может быть отклонено политиками DMARC.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 7: Что такое SendGrid и какие преимущества он предлагает

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

Цель: Очередь, retry и idempotency

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 8: Основные возможности и типы отправляемых сообщений через SendGrid

SPF, DKIM и DMARC подтверждают разрешённые источники и политику домена. Bounce и complaint должны менять дальнейшую стратегию отправки.

Nodemailer создаёт transporter один раз и переиспользует соединения там, где это поддерживает провайдер.

Цель: SPF, DKIM, DMARC, bounce и complaint

Бесконечный retry постоянной ошибки создаёт шум и блокирует очередь; нужны классификация и dead-letter.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 9: Обзор Nodemailer

Отправка письма — внешняя побочная операция. HTTP-запрос обычно фиксирует намерение в очереди, а worker выполняет доставку с retry и наблюдаемым статусом.

Idempotency key не даёт повторной доставке job отправить одинаковое транзакционное письмо несколько раз.

Цель: SMTP и API-провайдеры

Письмо с непроверенным адресом отправителя ломает репутацию домена и может быть отклонено политиками DMARC.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 10: Что такое Nodemailer и когда его стоит использовать

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

Цель: Шаблоны и text fallback

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 11: Основные возможности и типы отправляемых сообщений через Nodemailer

SPF, DKIM и DMARC подтверждают разрешённые источники и политику домена. Bounce и complaint должны менять дальнейшую стратегию отправки.

Nodemailer создаёт transporter один раз и переиспользует соединения там, где это поддерживает провайдер.

Цель: Очередь, retry и idempotency

Бесконечный retry постоянной ошибки создаёт шум и блокирует очередь; нужны классификация и dead-letter.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 12: Типы отправляемых сообщений

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Ресурс открывается как можно позже и закрывается детерминированно. Таймаут и отмена проходят через все поддерживающие их слои.

Цель: SPF, DKIM, DMARC, bounce и complaint

Обработка только happy path оставляет приложение без определённого поведения при сбое.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Дополнительный разбор 13: Практическая работа с Nodemailer

Письмо содержит text fallback и HTML-версию. Пользовательские значения экранируются, ссылки строятся из доверенной конфигурации.

Шаблон, локаль и данные версии фиксируются в задаче или воспроизводимо восстанавливаются при обработке.

Цель: SMTP и API-провайдеры

Ожидание SMTP в request path делает latency и доступность API зависимыми от почтового провайдера.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Итоговая работа

Соберите небольшой проект по теме урока. README проекта должен содержать назначение, требования, команды запуска, контракт входа и выхода, перечень ожидаемых ошибок и описание решения для graceful shutdown.

Минимальная проверка:

- успешный сценарий;
- неверный ввод;
- отказ внешней зависимости;
- timeout или отмена;
- повтор операции;
- завершение процесса;
- отсутствие секретов в логах и репозитории.

## Официальные источники

- [Nodemailer](https://nodemailer.com/)
- [SendGrid Node.js](https://www.twilio.com/docs/sendgrid/for-developers/sending-email/quickstart-nodejs)
- [Документация Node.js 24](https://nodejs.org/docs/latest-v24.x/api/)

- [Поддерживаемые версии Node.js](https://nodejs.org/en/about/previous-releases)
- [Node.js Learn](https://nodejs.org/en/learn)

## Критерии завершения урока

- Вы объясняете механизм без привязки к одному примеру.
- Вы понимаете границы встроенного API и внешней библиотеки.
- Вы умеете обработать ошибку, timeout и отмену.
- Вы можете проверить решение автоматическим тестом.
- Вы знаете, где найти статус и документацию использованного API.
