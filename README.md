# 5.3. MongoDB: документы, схема и моделирование

> Актуальная версия курса: Node.js 24 LTS, ECMAScript Modules и Express 5.

## О чём урок

Этот урок — самостоятельная глава курса. Материал построен от модели выполнения к проектным решениям: сначала разбираем, что делает платформа, затем пишем код, определяем отказоустойчивость и закрепляем результат практикой.

Старые примеры и устаревшие версии библиотек в эту редакцию не перенесены. Все объяснения сформулированы заново для современного Node.js.

## Как работать с материалом

1. Прочитайте раздел и сформулируйте его основную мысль своими словами.
2. До запуска примера предскажите результат и возможную ошибку.
3. Наберите код вручную, не вставляя его целиком.
4. Измените одно условие и объясните новое поведение.
5. Выполните мини-практику и ответьте на вопросы.
6. Сверьте нестабильный API с документацией Node.js 24.

## Требования

```bash
node --version
npm --version
```

Используйте Node.js 24.x. Новый код пишется в формате ESM. Встроенные модули импортируются с префиксом `node:`.

## 1. 5.3 MongoDB и Нормализация

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Опишите для темы «5.3 MongoDB и Нормализация» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 2. Введение в MongoDB

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Embedding против references

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Соберите минимальный пример по теме «Введение в MongoDB» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 3. Введение

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Отделите чистое преобразование по теме «Введение» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 4. История и популярность

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Опишите для темы «История и популярность» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 5. Особенности MongoDB

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Соберите минимальный пример по теме «Особенности MongoDB» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 6. Применение MongoDB

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Embedding против references

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Отделите чистое преобразование по теме «Применение MongoDB» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 7. Краткий обзор NoSQL баз данных. Чем MongoDB отличается от традиционных реляционных баз данных.

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Опишите для темы «Краткий обзор NoSQL баз данных. Чем MongoDB отличается от традиционных реляционных баз данных.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 8. Краткий обзор NoSQL баз данных

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

EXPLAIN ANALYZE выполняет запрос и показывает фактические строки и время. Его сравнивают с оценкой planner и проверяют на данных production-подобного объёма.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Соберите минимальный пример по теме «Краткий обзор NoSQL баз данных» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 9. Отличия MongoDB от традиционных реляционных баз данных

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Отделите чистое преобразование по теме «Отличия MongoDB от традиционных реляционных баз данных» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 10. Основные концепции MongoDB

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Embedding против references

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Опишите для темы «Основные концепции MongoDB» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 11. Различия между SQL и NoSQL запросами.

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Соберите минимальный пример по теме «Различия между SQL и NoSQL запросами.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 12. Документы и Коллекции

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Отделите чистое преобразование по теме «Документы и Коллекции» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 13. \id и BSON

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Опишите для темы «\id и BSON» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 14. Различия между SQL и NoSQL запросами

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Embedding против references

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Соберите минимальный пример по теме «Различия между SQL и NoSQL запросами» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 15. Базовые операции в MongoDB (CRUD)

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Отделите чистое преобразование по теме «Базовые операции в MongoDB (CRUD)» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 16. Создание базы данных и коллекций.

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Опишите для темы «Создание базы данных и коллекций.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 17. Шаг 1: Установка MongoDB Node.js Driver

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Соберите минимальный пример по теме «Шаг 1: Установка MongoDB Node.js Driver» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 18. Шаг 2: Подключение к MongoDB

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Embedding против references

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Отделите чистое преобразование по теме «Шаг 2: Подключение к MongoDB» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 19. Шаг 3: Создание базы данных и коллекции

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Уникальный индекс и обработка duplicate key необходимы даже при предварительной проверке существования.

### Применение

Опишите для темы «Шаг 3: Создание базы данных и коллекции» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 20. Шаг 4: Базовые операции (CRUD)

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Соберите минимальный пример по теме «Шаг 4: Базовые операции (CRUD)» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 21. Создание (Insert)

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Уникальный индекс и обработка duplicate key необходимы даже при предварительной проверке существования.

### Применение

Отделите чистое преобразование по теме «Создание (Insert)» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 22. Чтение (Find)

Для прикладного асинхронного кода используется `node:fs/promises`. Синхронные функции допустимы в коротком CLI или на этапе запуска, но не в горячем пути HTTP-запроса.

Метаданные `stat` могут измениться сразу после проверки. Для безопасности нельзя строить решение только на последовательности check-then-use.

В контексте этого урока раздел связан с задачей: Embedding против references

Наблюдение за каталогом зависит от ОС и не является гарантированной очередью бизнес-событий.

### Применение

Опишите для темы «Чтение (Find)» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 23. Обновление (Update)

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Уникальный индекс и обработка duplicate key необходимы даже при предварительной проверке существования.

### Применение

Соберите минимальный пример по теме «Обновление (Update)» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 24. Удаление (Delete)

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Отделите чистое преобразование по теме «Удаление (Delete)» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 25. Шаг 5: Запуск операций

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Уникальный индекс и обработка duplicate key необходимы даже при предварительной проверке существования.

### Применение

Опишите для темы «Шаг 5: Запуск операций» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 26. Шаг 6: Создание HTTP Сервера для Обработки Запросов

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

Keep-alive переиспользует TCP-соединение, но сервер всё равно должен ограничивать таймауты заголовков, запроса и простоя.

В контексте этого урока раздел связан с задачей: Embedding против references

Два вызова отправки ответа приводят к `headers already sent`; обычно это ошибка управления потоком.

### Применение

Соберите минимальный пример по теме «Шаг 6: Создание HTTP Сервера для Обработки Запросов» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 27. Тестирование через Postman

Coverage показывает непройденные строки и ветви, но не оценивает качество assertions. Сначала выбирают риск, затем сценарий.

Diagnostic report, inspector, CPU profile и heap snapshot отвечают на разные вопросы; инструмент выбирают после формулировки симптома.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Snapshot большого объекта скрывает значимое изменение среди шума; критичные поля лучше утверждать явно.

### Применение

Отделите чистое преобразование по теме «Тестирование через Postman» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 28. Нормализация SQL и NoSQL баз данных

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Опишите для темы «Нормализация SQL и NoSQL баз данных» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 29. Теория нормализации SQL

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Соберите минимальный пример по теме «Теория нормализации SQL» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 30. Цели нормализации. Понимание аномалий данных.

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Embedding против references

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Отделите чистое преобразование по теме «Цели нормализации. Понимание аномалий данных.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 31. Цели нормализации

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Опишите для темы «Цели нормализации» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 32. Аномалии данных

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Соберите минимальный пример по теме «Аномалии данных» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 33. Обзор нормальных форм от первой до третьей.

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Отделите чистое преобразование по теме «Обзор нормальных форм от первой до третьей.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 34. Первая Нормальная Форма (1NF)

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Embedding против references

Неявная зависимость усложняет тестирование и приводит к различиям между локальным и production-окружением.

### Применение

Опишите для темы «Первая Нормальная Форма (1NF)» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 35. Вторая Нормальная Форма (2NF)

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Соберите минимальный пример по теме «Вторая Нормальная Форма (2NF)» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 36. Третья Нормальная Форма (3NF)

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

EXPLAIN ANALYZE выполняет запрос и показывает фактические строки и время. Его сравнивают с оценкой planner и проверяют на данных production-подобного объёма.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Отделите чистое преобразование по теме «Третья Нормальная Форма (3NF)» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 37. Примеры

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Опишите для темы «Примеры» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 38. Пример 1NF

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Входные данные нормализуются на границе, бизнес-правила выполняются отдельно, а побочные эффекты получают явного владельца.

В контексте этого урока раздел связан с задачей: Embedding против references

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Соберите минимальный пример по теме «Пример 1NF» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 39. Пример 2NF

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Отделите чистое преобразование по теме «Пример 2NF» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 40. Пример 3NF

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Опишите для темы «Пример 3NF» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 41. Анализ таблиц на предмет нарушений нормальных форм.

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Документы и коллекции

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Соберите минимальный пример по теме «Анализ таблиц на предмет нарушений нормальных форм.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 42. Пример таблицы "Заказы и Продукты"

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Embedding против references

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Отделите чистое преобразование по теме «Пример таблицы "Заказы и Продукты"» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 43. Проблема 1: Повторяющиеся данные

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Опишите для темы «Проблема 1: Повторяющиеся данные» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 44. Проблема 2: Транзитивная зависимость

В CI используется чистая установка по lock-файлу. Глобальные инструменты не должны быть скрытым условием сборки: команда проекта запускается через npm script или `npx` с зафиксированной версией.

Современный Node.js предоставляет watch mode, env-file, test runner и `util.parseArgs`; небольшому проекту часто не нужны отдельные пакеты для этих задач.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Слишком широкий диапазон версий без тестов может незаметно изменить транзитивную зависимость при следующей установке.

### Применение

Соберите минимальный пример по теме «Проблема 2: Транзитивная зависимость» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 45. Как исправить нарушения нормальных форм

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Отделите чистое преобразование по теме «Как исправить нарушения нормальных форм» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 46. Таблица "Заказы"

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Embedding против references

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Опишите для темы «Таблица "Заказы"» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 47. Таблица "Продукты"

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Соберите минимальный пример по теме «Таблица "Продукты"» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 48. Практическое задание: Нормализация таблицы до третьей нормальной формы.

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Уникальный индекс и обработка duplicate key необходимы даже при предварительной проверке существования.

### Применение

Отделите чистое преобразование по теме «Практическое задание: Нормализация таблицы до третьей нормальной формы.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 49. Исходная таблица "Университет"

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Опишите для темы «Исходная таблица "Университет"» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 50. Шаг 1: Создание таблицы "Студенты"

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Embedding против references

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Соберите минимальный пример по теме «Шаг 1: Создание таблицы "Студенты"» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 51. Шаг 2: Создание таблицы "Курсы"

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Отделите чистое преобразование по теме «Шаг 2: Создание таблицы "Курсы"» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 52. Шаг 3: Создание таблицы "Оценки"

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

EXPLAIN ANALYZE выполняет запрос и показывает фактические строки и время. Его сравнивают с оценкой planner и проверяют на данных production-подобного объёма.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Опишите для темы «Шаг 3: Создание таблицы "Оценки"» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 53. Теория нормализации NoSQL в контексте MongoDB

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Уникальный индекс и обработка duplicate key необходимы даже при предварительной проверке существования.

### Применение

Соберите минимальный пример по теме «Теория нормализации NoSQL в контексте MongoDB» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 54. Понимание Нормализации в MongoDB

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Embedding против references

Уникальный индекс и обработка duplicate key необходимы даже при предварительной проверке существования.

### Применение

Отделите чистое преобразование по теме «Понимание Нормализации в MongoDB» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 55. Пример Нормализации в MongoDB

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Опишите для темы «Пример Нормализации в MongoDB» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 56. Нормализация:

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Соберите минимальный пример по теме «Нормализация:» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 57. Когда Нормализовать

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Отделите чистое преобразование по теме «Когда Нормализовать» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 58. Когда Использовать Денормализацию

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Embedding против references

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Опишите для темы «Когда Использовать Денормализацию» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 59. Применение MongoDB и Mongoose. Понимание нормализации в реальных проектах

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Соберите минимальный пример по теме «Применение MongoDB и Mongoose. Понимание нормализации в реальных проектах» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 60. Применение MongoDB и Mongoose в проектах

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Отделите чистое преобразование по теме «Применение MongoDB и Mongoose в проектах» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 61. Понимание нормализации в реальных проектах

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Опишите для темы «Понимание нормализации в реальных проектах» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 62. Обзор Mongoose для работы с MongoDB

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Embedding против references

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Соберите минимальный пример по теме «Обзор Mongoose для работы с MongoDB» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 63. Основные концепции Mongoose

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Отделите чистое преобразование по теме «Основные концепции Mongoose» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 64. Пример использования Mongoose

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Опишите для темы «Пример использования Mongoose» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 65. Приложение на Node.js для взаимодействия с книгами, используя MongoDB

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

Операция над одним документом атомарна. Многодокументная транзакция доступна, но не должна заменять подходящую модель данных.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Соберите минимальный пример по теме «Приложение на Node.js для взаимодействия с книгами, используя MongoDB» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 66. Шаг 1: Установка необходимых пакетов

Конфигурация приходит из окружения, но строки из `process.env` всё равно требуют преобразования и проверки. Ошибка обязательной настройки должна останавливать приложение при старте.

dependencies нужны приложению во время работы, devDependencies — для разработки и сборки, peerDependencies описывают совместимость с зависимостью потребителя.

В контексте этого урока раздел связан с задачей: Embedding против references

Слишком широкий диапазон версий без тестов может незаметно изменить транзитивную зависимость при следующей установке.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Установка необходимых пакетов» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 67. Шаг 2: Создание модели книги

Документная модель проектируется под операции чтения и записи, а не как автоматическая копия классов приложения. Решение embed или reference зависит от совместного чтения, размера и жизненного цикла данных.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Опишите для темы «Шаг 2: Создание модели книги» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 68. Шаг 3: Создание сервера

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Соберите минимальный пример по теме «Шаг 3: Создание сервера» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 69. Шаг 4: Запуск сервера

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Отделите чистое преобразование по теме «Шаг 4: Запуск сервера» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 70. Шаг 5: Улучшения структуры проекта и добавления дополнительных функций

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Embedding против references

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Опишите для темы «Шаг 5: Улучшения структуры проекта и добавления дополнительных функций» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 71. Шаг 6: Рефакторинг создания книги

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

Aggregation pipeline последовательно преобразует набор документов. Ранний `$match` и подходящий индекс уменьшают объём дальнейшей обработки.

В контексте этого урока раздел связан с задачей: Schema validation и Mongoose

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Соберите минимальный пример по теме «Шаг 6: Рефакторинг создания книги» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 72. Шаг 7: Изменение сервера для использования контроллеров

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Индексы, aggregation и транзакции

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Отделите чистое преобразование по теме «Шаг 7: Изменение сервера для использования контроллеров» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 73. Шаг 8: Запуск и тестирование

Тест проверяет наблюдаемое поведение и контракт ошибки, а не случайную внутреннюю последовательность вызовов. Это позволяет менять реализацию без переписывания всей suite.

Unit-тест изолирует чистую логику, integration-тест проверяет реальную границу, end-to-end проходит через публичный интерфейс.

В контексте этого урока раздел связан с задачей: Документы и коллекции

Snapshot большого объекта скрывает значимое изменение среди шума; критичные поля лучше утверждать явно.

### Применение

Опишите для темы «Шаг 8: Запуск и тестирование» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## Цельный пример урока

Ниже приведён актуальный минимальный пример. Он размещён прямо в материале, чтобы теория и код не расходились по отдельным каталогам.

### `model.mjs`

```js
import mongoose from 'mongoose';
const taskSchema = new mongoose.Schema({
  title: { type: String, required: true, trim: true, maxlength: 200 },
  completed: { type: Boolean, default: false, index: true },
  ownerId: { type: mongoose.Schema.Types.ObjectId, required: true, index: true },
}, { timestamps: true });
taskSchema.index({ ownerId: 1, createdAt: -1 });
export const Task = mongoose.model('Task', taskSchema);
```

### `package.json`

```json
{
  "name": "lesson-5-modern-example",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node model.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "mongoose": "^9.0.0"
  }
}
```

## Итоговая работа

Соберите небольшой проект по теме урока. README проекта должен содержать назначение, требования, команды запуска, контракт входа и выхода, перечень ожидаемых ошибок и описание решения для graceful shutdown.

Минимальная проверка:

- успешный сценарий;
- неверный ввод;
- отказ внешней зависимости;
- timeout или отмена;
- повтор операции;
- завершение процесса;
- отсутствие секретов в логах и репозитории.

## Официальные источники

- [MongoDB data modeling](https://www.mongodb.com/docs/manual/data-modeling/)
- [Mongoose](https://mongoosejs.com/docs/)
- [Документация Node.js 24](https://nodejs.org/docs/latest-v24.x/api/)

- [Поддерживаемые версии Node.js](https://nodejs.org/en/about/previous-releases)
- [Node.js Learn](https://nodejs.org/en/learn)

## Критерии завершения урока

- Вы объясняете механизм без привязки к одному примеру.
- Вы понимаете границы встроенного API и внешней библиотеки.
- Вы умеете обработать ошибку, timeout и отмену.
- Вы можете проверить решение автоматическим тестом.
- Вы знаете, где найти статус и документацию использованного API.
