# 5.1. PostgreSQL: модель данных, SQL и безопасное подключение

> Актуальная версия курса: Node.js 24 LTS, ECMAScript Modules и Express 5.

## О чём урок

Этот урок — самостоятельная глава курса. Материал построен от модели выполнения к проектным решениям: сначала разбираем, что делает платформа, затем пишем код, определяем отказоустойчивость и закрепляем результат практикой.

Старые примеры и устаревшие версии библиотек в эту редакцию не перенесены. Все объяснения сформулированы заново для современного Node.js.

## Как работать с материалом

1. Прочитайте раздел и сформулируйте его основную мысль своими словами.
2. До запуска примера предскажите результат и возможную ошибку.
3. Наберите код вручную, не вставляя его целиком.
4. Измените одно условие и объясните новое поведение.
5. Выполните мини-практику и ответьте на вопросы.
6. Сверьте нестабильный API с документацией Node.js 24.

## Требования

```bash
node --version
npm --version
```

Используйте Node.js 24.x. Новый код пишется в формате ESM. Встроенные модули импортируются с префиксом `node:`.

## 1. Введение в базы данных и практика с PostgreSQL

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Опишите для темы «Введение в базы данных и практика с PostgreSQL» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 2. Теория баз данных

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Соберите минимальный пример по теме «Теория баз данных» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 3. История и этапы развития баз данных.

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Отделите чистое преобразование по теме «История и этапы развития баз данных.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 4. Ранние годы (1950-е - 1960-е)

package.json описывает контракт запуска: формат модулей, scripts, поддерживаемую Node.js и прямые зависимости. Lock-файл фиксирует полный граф и делает установку воспроизводимой.

SemVer помогает оценивать диапазон обновлений, но не доказывает совместимость. Изменение lock-файла проверяют так же внимательно, как изменение исходного кода.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

Секрет в репозитории остаётся скомпрометированным даже после удаления из последнего коммита; его необходимо отозвать и заменить.

### Применение

Опишите для темы «Ранние годы (1950-е - 1960-е)» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 5. Развитие реляционных баз данных (1970-е)

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Соберите минимальный пример по теме «Развитие реляционных баз данных (1970-е)» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 6. Расцвет реляционных СУБД (1980-е - 1990-е)

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Отделите чистое преобразование по теме «Расцвет реляционных СУБД (1980-е - 1990-е)» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 7. Появление объектно-ориентированных и NoSQL баз данных (2000-е)

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

EXPLAIN ANALYZE выполняет запрос и показывает фактические строки и время. Его сравнивают с оценкой planner и проверяют на данных production-подобного объёма.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Опишите для темы «Появление объектно-ориентированных и NoSQL баз данных (2000-е)» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 8. Современные тенденции (2010-е и далее)

В CI используется чистая установка по lock-файлу. Глобальные инструменты не должны быть скрытым условием сборки: команда проекта запускается через npm script или `npx` с зафиксированной версией.

Современный Node.js предоставляет watch mode, env-file, test runner и `util.parseArgs`; небольшому проекту часто не нужны отдельные пакеты для этих задач.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

Секрет в репозитории остаётся скомпрометированным даже после удаления из последнего коммита; его необходимо отозвать и заменить.

### Применение

Соберите минимальный пример по теме «Современные тенденции (2010-е и далее)» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 9. Основные понятия баз данных и систем управления базами данных.

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

EXPLAIN ANALYZE выполняет запрос и показывает фактические строки и время. Его сравнивают с оценкой planner и проверяют на данных production-подобного объёма.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Отделите чистое преобразование по теме «Основные понятия баз данных и систем управления базами данных.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 10. База данных (БД)

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Опишите для темы «База данных (БД)» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 11. Система управления базами данных (СУБД)

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Соберите минимальный пример по теме «Система управления базами данных (СУБД)» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 12. Модели данных

Конфигурация приходит из окружения, но строки из `process.env` всё равно требуют преобразования и проверки. Ошибка обязательной настройки должна останавливать приложение при старте.

dependencies нужны приложению во время работы, devDependencies — для разработки и сборки, peerDependencies описывают совместимость с зависимостью потребителя.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

Слишком широкий диапазон версий без тестов может незаметно изменить транзитивную зависимость при следующей установке.

### Применение

Отделите чистое преобразование по теме «Модели данных» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 13. Транзакции

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Опишите для темы «Транзакции» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 14. SQL (Structured Query Language)

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Соберите минимальный пример по теме «SQL (Structured Query Language)» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 15. Нормализация

Индекс создают под фильтр и сортировку конкретного запроса. Его эффективность проверяют через explain, размер и влияние на запись.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Populate в цикле может скрыть N+1 и передать клиенту больше полей, чем разрешено.

### Применение

Отделите чистое преобразование по теме «Нормализация» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 16. Индексы

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

EXPLAIN ANALYZE выполняет запрос и показывает фактические строки и время. Его сравнивают с оценкой planner и проверяют на данных production-подобного объёма.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Опишите для темы «Индексы» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 17. Обзор различных моделей баз данных: файловая, сетевая, иерархическая, реляционная, объектно-ориентированная.

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

EXPLAIN ANALYZE выполняет запрос и показывает фактические строки и время. Его сравнивают с оценкой planner и проверяют на данных production-подобного объёма.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Соберите минимальный пример по теме «Обзор различных моделей баз данных: файловая, сетевая, иерархическая, реляционная, объектно-ориентированная.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 18. Файловая модель

Запись важного файла проектируют с учётом сбоя посередине: временный файл, проверка результата и атомарный rename уменьшают вероятность повреждения.

Пользовательский относительный путь нормализуют, разрешают внутри доверенного корня и повторно проверяют итоговую границу.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

Параллельная запись в один файл без координации приводит к потерянным обновлениям.

### Применение

Отделите чистое преобразование по теме «Файловая модель» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 19. Сетевая модель

Безопасность начинается с модели угроз: какой актив защищается, кто атакует, через какую границу и с каким возможным ущербом. Middleware выбирают после ответа на эти вопросы.

CSRF актуален, когда браузер автоматически прикладывает cookie. Защита сочетает SameSite, проверку Origin/Referer и token для изменяющих запросов.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Уязвимая или заброшенная зависимость остаётся частью attack surface даже при правильном собственном коде.

### Применение

Опишите для темы «Сетевая модель» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 20. Иерархическая модель

В CI используется чистая установка по lock-файлу. Глобальные инструменты не должны быть скрытым условием сборки: команда проекта запускается через npm script или `npx` с зафиксированной версией.

dependencies нужны приложению во время работы, devDependencies — для разработки и сборки, peerDependencies описывают совместимость с зависимостью потребителя.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

Пакет с именем встроенного модуля, например `fs` или `path`, не заменяет API Node.js и обычно вообще не должен устанавливаться.

### Применение

Соберите минимальный пример по теме «Иерархическая модель» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 21. Реляционная модель

Современное решение должно быть проверяемым, наблюдаемым и безопасным по умолчанию. Удобство демонстрации не должно скрывать production-ограничения.

Входные данные нормализуются на границе, бизнес-правила выполняются отдельно, а побочные эффекты получают явного владельца.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

Обработка только happy path оставляет приложение без определённого поведения при сбое.

### Применение

Отделите чистое преобразование по теме «Реляционная модель» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 22. Объектно-ориентированная модель

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Опишите для темы «Объектно-ориентированная модель» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 23. Язык SQL

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Соберите минимальный пример по теме «Язык SQL» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 24. Почему SQL?

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Отделите чистое преобразование по теме «Почему SQL?» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 25. Структура языка

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

Обработка только happy path оставляет приложение без определённого поведения при сбое.

### Применение

Опишите для темы «Структура языка» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 26. Операторы определения данных (DDL)

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

EXPLAIN ANALYZE выполняет запрос и показывает фактические строки и время. Его сравнивают с оценкой planner и проверяют на данных production-подобного объёма.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Соберите минимальный пример по теме «Операторы определения данных (DDL)» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 27. Операторы манипуляции данными (DML)

Валидация подтверждает форму данных, параметризация защищает запрос, escaping защищает контекст вывода. Один слой не заменяет остальные.

Rate limit выбирается по идентичности и стоимости операции; общий IP-лимит за NAT может блокировать множество честных пользователей.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Уязвимая или заброшенная зависимость остаётся частью attack surface даже при правильном собственном коде.

### Применение

Отделите чистое преобразование по теме «Операторы манипуляции данными (DML)» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 28. Операторы определения доступа к данным (DCL)

package.json описывает контракт запуска: формат модулей, scripts, поддерживаемую Node.js и прямые зависимости. Lock-файл фиксирует полный граф и делает установку воспроизводимой.

dependencies нужны приложению во время работы, devDependencies — для разработки и сборки, peerDependencies описывают совместимость с зависимостью потребителя.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

Секрет в репозитории остаётся скомпрометированным даже после удаления из последнего коммита; его необходимо отозвать и заменить.

### Применение

Опишите для темы «Операторы определения доступа к данным (DCL)» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 29. Синтаксис

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Входные данные нормализуются на границе, бизнес-правила выполняются отдельно, а побочные эффекты получают явного владельца.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

Неявная зависимость усложняет тестирование и приводит к различиям между локальным и production-окружением.

### Применение

Соберите минимальный пример по теме «Синтаксис» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 30. Практика с PostgreSQL через Node.js

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Отделите чистое преобразование по теме «Практика с PostgreSQL через Node.js» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 31. Установка и настройка PostgreSQL.

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Опишите для темы «Установка и настройка PostgreSQL.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 32. Установка на Windows

В CI используется чистая установка по lock-файлу. Глобальные инструменты не должны быть скрытым условием сборки: команда проекта запускается через npm script или `npx` с зафиксированной версией.

SemVer помогает оценивать диапазон обновлений, но не доказывает совместимость. Изменение lock-файла проверяют так же внимательно, как изменение исходного кода.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

Секрет в репозитории остаётся скомпрометированным даже после удаления из последнего коммита; его необходимо отозвать и заменить.

### Применение

Соберите минимальный пример по теме «Установка на Windows» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 33. Установка на macOS

Современное решение должно быть проверяемым, наблюдаемым и безопасным по умолчанию. Удобство демонстрации не должно скрывать production-ограничения.

Ресурс открывается как можно позже и закрывается детерминированно. Таймаут и отмена проходят через все поддерживающие их слои.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

Неявная зависимость усложняет тестирование и приводит к различиям между локальным и production-окружением.

### Применение

Отделите чистое преобразование по теме «Установка на macOS» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 34. Установка на Linux (Debian/Ubuntu)

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

EXPLAIN ANALYZE выполняет запрос и показывает фактические строки и время. Его сравнивают с оценкой planner и проверяют на данных production-подобного объёма.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Опишите для темы «Установка на Linux (Debian/Ubuntu)» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 35. Общие шаги после установки

Helmet устанавливает набор защитных заголовков, CORS управляет чтением ответа браузерным origin, rate limiter ограничивает частоту. Эти механизмы не заменяют authentication и authorization.

Rate limit выбирается по идентичности и стоимости операции; общий IP-лимит за NAT может блокировать множество честных пользователей.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Разрешение CORS `*` вместе с неверной моделью credentials либо не работает, либо открывает лишний доступ.

### Применение

Соберите минимальный пример по теме «Общие шаги после установки» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 36. Настройка проекта для работы с PostgreSQL.

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

EXPLAIN ANALYZE выполняет запрос и показывает фактические строки и время. Его сравнивают с оценкой planner и проверяют на данных production-подобного объёма.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Отделите чистое преобразование по теме «Настройка проекта для работы с PostgreSQL.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 37. Настройка среды Node.js:

Node.js — это среда выполнения JavaScript, а не язык и не веб-фреймворк. V8 компилирует и исполняет JavaScript, Node добавляет системные API, а libuv участвует в организации цикла событий и части фоновой работы.

JavaScript выполняется до ближайшей точки возврата управления. Затем runtime может обработать microtasks, перейти к следующей фазе event loop и вызвать готовые callbacks.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

Глобальное изменяемое состояние усложняет тестирование, создаёт неявные зависимости и расходится между несколькими экземплярами приложения.

### Применение

Опишите для темы «Настройка среды Node.js:» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 38. Установка библиотеки для работы с PostgreSQL:

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Соберите минимальный пример по теме «Установка библиотеки для работы с PostgreSQL:» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 39. Настройка подключения к базе данных:

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Отделите чистое преобразование по теме «Настройка подключения к базе данных:» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 40. Работа с базой данных в вашем приложении:

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Опишите для темы «Работа с базой данных в вашем приложении:» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 41. Создание таблиц: первичный ключ, уникальность, значения по умолчанию.

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Соберите минимальный пример по теме «Создание таблиц: первичный ключ, уникальность, значения по умолчанию.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 42. Чтобы создать новую таблицу в pgAdmin и просмотреть её, вы можете следовать этим шагам:

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

N+1 выполняет отдельный запрос для каждого элемента коллекции; решение — JOIN, batch query или осознанный loader.

### Применение

Отделите чистое преобразование по теме «Чтобы создать новую таблицу в pgAdmin и просмотреть её, вы можете следовать этим шагам:» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 43. Первичный ключ (Primary Key)

Безопасность начинается с модели угроз: какой актив защищается, кто атакует, через какую границу и с каким возможным ущербом. Middleware выбирают после ответа на эти вопросы.

Rate limit выбирается по идентичности и стоимости операции; общий IP-лимит за NAT может блокировать множество честных пользователей.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Секреты в логах, URL или exception message часто обходят все криптографические меры.

### Применение

Опишите для темы «Первичный ключ (Primary Key)» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 44. Уникальность (Unique Constraint)

В CI используется чистая установка по lock-файлу. Глобальные инструменты не должны быть скрытым условием сборки: команда проекта запускается через npm script или `npx` с зафиксированной версией.

Современный Node.js предоставляет watch mode, env-file, test runner и `util.parseArgs`; небольшому проекту часто не нужны отдельные пакеты для этих задач.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

Слишком широкий диапазон версий без тестов может незаметно изменить транзитивную зависимость при следующей установке.

### Применение

Соберите минимальный пример по теме «Уникальность (Unique Constraint)» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 45. Значения по умолчанию (Default Values)

Современное решение должно быть проверяемым, наблюдаемым и безопасным по умолчанию. Удобство демонстрации не должно скрывать production-ограничения.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Отделите чистое преобразование по теме «Значения по умолчанию (Default Values)» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 46. Давайте рассмотрим реальный пример создания таблицы в PostgreSQL.

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Опишите для темы «Давайте рассмотрим реальный пример создания таблицы в PostgreSQL.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 47. Основы типов данных и индексов.

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Соберите минимальный пример по теме «Основы типов данных и индексов.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 48. Основы Типов Данных в PostgreSQL

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Отделите чистое преобразование по теме «Основы Типов Данных в PostgreSQL» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 49. Индексы в PostgreSQL

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Опишите для темы «Индексы в PostgreSQL» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 50. Расширение Существующей Таблицы

Параметризованный запрос отделяет SQL-код от значений и является базовой защитой от injection. Имена таблиц и направления сортировки параметрами не становятся — их выбирают из allowlist.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Соберите минимальный пример по теме «Расширение Существующей Таблицы» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 51. Модификация Таблицы и Добавление Индексов

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

JOIN формирует строки на основе отношения таблиц. До написания запроса важно определить cardinality, иначе агрегаты легко умножатся.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Отделите чистое преобразование по теме «Модификация Таблицы и Добавление Индексов» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 52. Практика выполнения запросов SELECT, INSERT, UPDATE, DELETE через Node.js.

Node.js — это среда выполнения JavaScript, а не язык и не веб-фреймворк. V8 компилирует и исполняет JavaScript, Node добавляет системные API, а libuv участвует в организации цикла событий и части фоновой работы.

Состояние процесса включает аргументы запуска, переменные среды, открытые handles, память и код завершения. Эти части надо проектировать так же внимательно, как HTTP-маршруты.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

Использование EOL-версии оставляет приложение без регулярных исправлений безопасности, даже если сам код продолжает запускаться.

### Применение

Опишите для темы «Практика выполнения запросов SELECT, INSERT, UPDATE, DELETE через Node.js.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 53. Выполнение Запросов

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

Обработка только happy path оставляет приложение без определённого поведения при сбое.

### Применение

Соберите минимальный пример по теме «Выполнение Запросов» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 54. Шаг 1: Настройка и Установка

Транзакция объединяет изменения в атомарную единицу. В node-postgres все её запросы выполняются на одном client, полученном из пула.

EXPLAIN ANALYZE выполняет запрос и показывает фактические строки и время. Его сравнивают с оценкой planner и проверяют на данных production-подобного объёма.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

Пул без лимита переносит перегрузку приложения на базу. Очередь ожидания и timeout должны быть наблюдаемыми.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Настройка и Установка» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 55. Шаг 2: Настройка PostgreSQL

Схема базы фиксирует инварианты ближе всего к данным: типы, NOT NULL, UNIQUE, CHECK и внешние ключи защищают все пути записи, а не только один HTTP-handler.

Индекс ускоряет конкретный способ чтения ценой диска и дополнительной работы каждой записи. Порядок колонок составного индекса выбирают по реальным предикатам и сортировке.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Длинная транзакция удерживает locks и старые версии строк, увеличивает конфликтность и мешает обслуживанию.

### Применение

Опишите для темы «Шаг 2: Настройка PostgreSQL» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 56. Шаг 3: Создание HTTP Сервера

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: Миграции и переменные среды

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Соберите минимальный пример по теме «Шаг 3: Создание HTTP Сервера» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 57. Шаг 4: Запуск Сервера

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: Реляционная модель, ключи и ограничения

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Отделите чистое преобразование по теме «Шаг 4: Запуск Сервера» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 58. Тестирование через Postman

Тест проверяет наблюдаемое поведение и контракт ошибки, а не случайную внутреннюю последовательность вызовов. Это позволяет менять реализацию без переписывания всей suite.

Dependency injection часов, random и fetch делает тест детерминированным без глобальной подмены runtime.

В контексте этого урока раздел связан с задачей: Пул соединений и транзакции

Тест с реальным временем, случайностью или интернетом становится flaky и теряет доверие команды.

### Применение

Опишите для темы «Тестирование через Postman» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 59. Примечания

Helmet устанавливает набор защитных заголовков, CORS управляет чтением ответа браузерным origin, rate limiter ограничивает частоту. Эти механизмы не заменяют authentication и authorization.

TLS обычно завершается на reverse proxy или load balancer. Приложение доверяет forwarded-заголовкам только от явно настроенных прокси.

В контексте этого урока раздел связан с задачей: Параметризованные запросы против SQL injection

Разрешение CORS `*` вместе с неверной моделью credentials либо не работает, либо открывает лишний доступ.

### Применение

Соберите минимальный пример по теме «Примечания» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## Цельный пример урока

Ниже приведён актуальный минимальный пример. Он размещён прямо в материале, чтобы теория и код не расходились по отдельным каталогам.

### `package.json`

```json
{
  "name": "lesson-5-modern-example",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node schema.sql",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
```

### `queries.mjs`

```js
import pg from 'pg';
const pool = new pg.Pool({ connectionString: process.env.DATABASE_URL });
const { rows } = await pool.query(
  'SELECT id, title, completed FROM tasks WHERE completed = $1 ORDER BY id',
  [false],
);
console.table(rows);
await pool.end();
```

### `schema.sql`

```sql
CREATE TABLE tasks (
  id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  title text NOT NULL CHECK (char_length(title) BETWEEN 1 AND 200),
  completed boolean NOT NULL DEFAULT false,
  created_at timestamptz NOT NULL DEFAULT now()
);
```

## Итоговая работа

Соберите небольшой проект по теме урока. README проекта должен содержать назначение, требования, команды запуска, контракт входа и выхода, перечень ожидаемых ошибок и описание решения для graceful shutdown.

Минимальная проверка:

- успешный сценарий;
- неверный ввод;
- отказ внешней зависимости;
- timeout или отмена;
- повтор операции;
- завершение процесса;
- отсутствие секретов в логах и репозитории.

## Официальные источники

- [node-postgres](https://node-postgres.com/)
- [PostgreSQL tutorial](https://www.postgresql.org/docs/current/tutorial.html)
- [Документация Node.js 24](https://nodejs.org/docs/latest-v24.x/api/)

- [Поддерживаемые версии Node.js](https://nodejs.org/en/about/previous-releases)
- [Node.js Learn](https://nodejs.org/en/learn)

## Критерии завершения урока

- Вы объясняете механизм без привязки к одному примеру.
- Вы понимаете границы встроенного API и внешней библиотеки.
- Вы умеете обработать ошибку, timeout и отмену.
- Вы можете проверить решение автоматическим тестом.
- Вы знаете, где найти статус и документацию использованного API.
