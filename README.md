# 6.7. Производительность, наблюдаемость и надёжность

> Актуальная версия курса: Node.js 24 LTS, ECMAScript Modules и Express 5.

## О чём урок

Этот урок — самостоятельная глава курса. Материал построен от модели выполнения к проектным решениям: сначала разбираем, что делает платформа, затем пишем код, определяем отказоустойчивость и закрепляем результат практикой.

Старые примеры и устаревшие версии библиотек в эту редакцию не перенесены. Все объяснения сформулированы заново для современного Node.js.

## Как работать с материалом

1. Прочитайте раздел и сформулируйте его основную мысль своими словами.
2. До запуска примера предскажите результат и возможную ошибку.
3. Наберите код вручную, не вставляя его целиком.
4. Измените одно условие и объясните новое поведение.
5. Выполните мини-практику и ответьте на вопросы.
6. Сверьте нестабильный API с документацией Node.js 24.

## Требования

```bash
node --version
npm --version
```

Используйте Node.js 24.x. Новый код пишется в формате ESM. Встроенные модули импортируются с префиксом `node:`.

## 1. Оптимизация и управление производительностью

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Опишите для темы «Оптимизация и управление производительностью» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 2. Принципы оптимизации производительности Express-приложений

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Соберите минимальный пример по теме «Принципы оптимизации производительности Express-приложений» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 3. Минимизация работы на стороне сервера

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Отделите чистое преобразование по теме «Минимизация работы на стороне сервера» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 4. Объяснение важности эффективного использования мидлваров

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Ресурс открывается как можно позже и закрывается детерминированно. Таймаут и отмена проходят через все поддерживающие их слои.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Неявная зависимость усложняет тестирование и приводит к различиям между локальным и production-окружением.

### Применение

Опишите для темы «Объяснение важности эффективного использования мидлваров» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 5. Примеры кода, демонстрирующие правильное и неправильное использование мидлваров

Promise представляет будущий результат, а `async/await` делает последовательность операций читаемой. Ошибка rejected Promise должна иметь владельца: обработчик запроса, задача очереди или верхнеуровневая политика завершения.

Callback API часто использует соглашение error-first. При переводе в Promise важно сохранить ошибку, не вызвать resolve дважды и корректно освободить ресурсы.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Забытый `await` превращает ожидаемую ошибку в необработанный rejection и может завершить запрос раньше фактической операции.

### Применение

Соберите минимальный пример по теме «Примеры кода, демонстрирующие правильное и неправильное использование мидлваров» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 6. Объяснение важности эффективного использования мидлваров.

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Отделите чистое преобразование по теме «Объяснение важности эффективного использования мидлваров.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 7. Важность эффективного использования мидлваров

Production-сборка должна быть воспроизводимой: поддерживаемая Node.js, lock-файл, минимальный артефакт и известная команда запуска.

Контейнер запускается не от root, получает секреты во время исполнения и содержит только необходимые production-зависимости.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Жёстко записанный секрет попадает в image layers и остаётся там после удаления из последнего слоя.

### Применение

Опишите для темы «Важность эффективного использования мидлваров» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 8. Примеры кода, демонстрирующие правильное и неправильное использование мидлваров.

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Неявная зависимость усложняет тестирование и приводит к различиям между локальным и production-окружением.

### Применение

Соберите минимальный пример по теме «Примеры кода, демонстрирующие правильное и неправильное использование мидлваров.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 9. Правильное использование мидлваров

Отмена является частью контракта операции. AbortSignal надо передавать вниз по слоям, чтобы остановить HTTP-запрос, таймер или другую поддерживаемую работу, когда результат больше не нужен.

Worker Thread имеет собственный JavaScript isolate. Передача больших данных может стоить дорого, поэтому оценивают сериализацию, Transferable objects и размер пула.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Забытый `await` превращает ожидаемую ошибку в необработанный rejection и может завершить запрос раньше фактической операции.

### Применение

Отделите чистое преобразование по теме «Правильное использование мидлваров» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 10. Неправильное использование мидлваров

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Опишите для темы «Неправильное использование мидлваров» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 11. Создание небольшого проекта на Node.js

При разборе поведения Node.js полезно разделять JavaScript-стек, очереди задач, операции операционной системы и пул libuv. Это защищает от ошибочного объяснения, будто любой асинхронный вызов автоматически создаёт отдельный JavaScript-поток.

JavaScript выполняется до ближайшей точки возврата управления. Затем runtime может обработать microtasks, перейти к следующей фазе event loop и вызвать готовые callbacks.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Глобальное изменяемое состояние усложняет тестирование, создаёт неявные зависимости и расходится между несколькими экземплярами приложения.

### Применение

Соберите минимальный пример по теме «Создание небольшого проекта на Node.js» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 12. Шаг 1: Создание проекта и установка зависимостей

Конфигурация приходит из окружения, но строки из `process.env` всё равно требуют преобразования и проверки. Ошибка обязательной настройки должна останавливать приложение при старте.

dependencies нужны приложению во время работы, devDependencies — для разработки и сборки, peerDependencies описывают совместимость с зависимостью потребителя.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Слишком широкий диапазон версий без тестов может незаметно изменить транзитивную зависимость при следующей установке.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Создание проекта и установка зависимостей» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 13. Шаг 2: Создание сервера с маршрутами

HTTP-запрос состоит из метода, target, заголовков и необязательного тела; ответ — из статуса, заголовков и тела. Сервер должен завершить каждую ветку ровно одним ответом.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Опишите для темы «Шаг 2: Создание сервера с маршрутами» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 14. Шаг 3: Запуск сервера

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Соберите минимальный пример по теме «Шаг 3: Запуск сервера» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 15. Шаг 4: Тестирование через Postman

Coverage показывает непройденные строки и ветви, но не оценивает качество assertions. Сначала выбирают риск, затем сценарий.

Dependency injection часов, random и fetch делает тест детерминированным без глобальной подмены runtime.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Чрезмерный mock проверяет собственные ожидания теста, а не совместимость с реальной системой.

### Применение

Отделите чистое преобразование по теме «Шаг 4: Тестирование через Postman» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 16. Заключение

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Опишите для темы «Заключение» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 17. Тестирование через Postman

Встроенный `node:test` поддерживает suites, hooks, mocking, watch mode и coverage. Для большинства учебных примеров отдельный test framework не обязателен.

Diagnostic report, inspector, CPU profile и heap snapshot отвечают на разные вопросы; инструмент выбирают после формулировки симптома.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Чрезмерный mock проверяет собственные ожидания теста, а не совместимость с реальной системой.

### Применение

Соберите минимальный пример по теме «Тестирование через Postman» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 18. Оптимизация маршрутизации

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Отделите чистое преобразование по теме «Оптимизация маршрутизации» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 19. Как Express обрабатывает маршруты

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Опишите для темы «Как Express обрабатывает маршруты» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 20. Влияние на производительность

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Логирование всего тела запроса увеличивает стоимость, шум и риск утечки чувствительных данных.

### Применение

Соберите минимальный пример по теме «Влияние на производительность» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 21. Стратегии оптимизации

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Логирование всего тела запроса увеличивает стоимость, шум и риск утечки чувствительных данных.

### Применение

Отделите чистое преобразование по теме «Стратегии оптимизации» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 22. Разбор как Express обрабатывает маршруты и как это влияет на производительность.

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Логирование всего тела запроса увеличивает стоимость, шум и риск утечки чувствительных данных.

### Применение

Опишите для темы «Разбор как Express обрабатывает маршруты и как это влияет на производительность.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 23. Практическое задание: оптимизация маршрутизации в существующем приложении.

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Логирование всего тела запроса увеличивает стоимость, шум и риск утечки чувствительных данных.

### Применение

Соберите минимальный пример по теме «Практическое задание: оптимизация маршрутизации в существующем приложении.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 24. Шаг 1: Создание базового приложения

Современное решение должно быть проверяемым, наблюдаемым и безопасным по умолчанию. Удобство демонстрации не должно скрывать production-ограничения.

Ресурс открывается как можно позже и закрывается детерминированно. Таймаут и отмена проходят через все поддерживающие их слои.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Обработка только happy path оставляет приложение без определённого поведения при сбое.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Создание базового приложения» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 25. Начальное приложение (server.js)

Асинхронность описывает возможность продолжить полезную работу во время ожидания, а параллельность — одновременное исполнение на нескольких вычислительных ресурсах. В Node.js эти понятия связаны, но не тождественны.

Worker Thread имеет собственный JavaScript isolate. Передача больших данных может стоить дорого, поэтому оценивают сериализацию, Transferable objects и размер пула.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Продолжение работы после `uncaughtException` опасно: состояние процесса может быть повреждено. Процесс корректно завершают и доверяют перезапуск supervisor-платформе.

### Применение

Опишите для темы «Начальное приложение (server.js)» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 26. Шаг 2: Оптимизация маршрутизации

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Логирование всего тела запроса увеличивает стоимость, шум и риск утечки чувствительных данных.

### Применение

Соберите минимальный пример по теме «Шаг 2: Оптимизация маршрутизации» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 27. Создание роутеров

Liveness отвечает, жив ли процесс; readiness — способен ли экземпляр принимать трафик. Эти проверки не должны выполнять дорогой полный бизнес-сценарий.

Контейнер запускается не от root, получает секреты во время исполнения и содержит только необходимые production-зависимости.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Неверный `trust proxy` позволяет подделать протокол или IP и нарушает secure cookie и rate limiting.

### Применение

Отделите чистое преобразование по теме «Создание роутеров» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 28. Обновление основного файла приложения (server.js)

Для прикладного асинхронного кода используется `node:fs/promises`. Синхронные функции допустимы в коротком CLI или на этапе запуска, но не в горячем пути HTTP-запроса.

FileHandle надо закрывать детерминированно. Высокоуровневые функции `readFile` и `writeFile` управляют handle сами, а низкоуровневый `open` передаёт ответственность вызывающему коду.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Path traversal позволяет выйти из разрешённого каталога через `..`, абсолютный путь или особенности платформы.

### Применение

Опишите для темы «Обновление основного файла приложения (server.js)» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 29. Использование кэширования и компрессии для улучшения скорости загрузки

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Соберите минимальный пример по теме «Использование кэширования и компрессии для улучшения скорости загрузки» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 30. Стратегии кэширования

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Отделите чистое преобразование по теме «Стратегии кэширования» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 31. 1. Кэширование статических файлов

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Опишите для темы «1. Кэширование статических файлов» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 32. 2. HTTP-заголовки для контроля кэширования

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Соберите минимальный пример по теме «2. HTTP-заголовки для контроля кэширования» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 33. 3. Кэширование ответов API

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Отделите чистое преобразование по теме «3. Кэширование ответов API» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 34. Реальные примеры применения

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Опишите для темы «Реальные примеры применения» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 35. 1. Кэширование статических файлов с использованием express.static

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Соберите минимальный пример по теме «1. Кэширование статических файлов с использованием express.static» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 36. 2. Настройка HTTP-заголовков для контроля кэширования

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Отделите чистое преобразование по теме «2. Настройка HTTP-заголовков для контроля кэширования» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 37. 3. Кэширование ответов API на стороне сервера с apicache

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Опишите для темы «3. Кэширование ответов API на стороне сервера с apicache» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 38. Введение в кэширование: client-side и server-side кэширование.

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Соберите минимальный пример по теме «Введение в кэширование: client-side и server-side кэширование.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 39. Client-side кэширование

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Отделите чистое преобразование по теме «Client-side кэширование» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 40. Примеры

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Ресурс открывается как можно позже и закрывается детерминированно. Таймаут и отмена проходят через все поддерживающие их слои.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Опишите для темы «Примеры» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 41. Server-side кэширование

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Соберите минимальный пример по теме «Server-side кэширование» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 42. Реализация кэширования статических файлов с использованием Express.static.

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Отделите чистое преобразование по теме «Реализация кэширования статических файлов с использованием Express.static.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 43. Базовое использование express.static

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Опишите для темы «Базовое использование express.static» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 44. Настройка кэширования

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Соберите минимальный пример по теме «Настройка кэширования» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 45. Пример с кэшированием

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Отделите чистое преобразование по теме «Пример с кэшированием» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 46. Важные соображения

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Логирование всего тела запроса увеличивает стоимость, шум и риск утечки чувствительных данных.

### Применение

Опишите для темы «Важные соображения» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 47. Компрессия данных

OpenAPI описывает операции, schemas, security и ошибки как машиночитаемый контракт. Swagger UI является представлением контракта, а не заменой самой спецификации.

При SIGTERM приложение снимает readiness, перестаёт принимать новые соединения, ждёт активные запросы в пределах deadline и закрывает ресурсы.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Неверный `trust proxy` позволяет подделать протокол или IP и нарушает secure cookie и rate limiting.

### Применение

Соберите минимальный пример по теме «Компрессия данных» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 48. Gzip vs. Deflate

Современное решение должно быть проверяемым, наблюдаемым и безопасным по умолчанию. Удобство демонстрации не должно скрывать production-ограничения.

Входные данные нормализуются на границе, бизнес-правила выполняются отдельно, а побочные эффекты получают явного владельца.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Обработка только happy path оставляет приложение без определённого поведения при сбое.

### Применение

Отделите чистое преобразование по теме «Gzip vs. Deflate» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 49. Настройка компрессии в Express

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Опишите для темы «Настройка компрессии в Express» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 50. Установка compression

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Логирование всего тела запроса увеличивает стоимость, шум и риск утечки чувствительных данных.

### Применение

Соберите минимальный пример по теме «Установка compression» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 51. Пример использования

Liveness отвечает, жив ли процесс; readiness — способен ли экземпляр принимать трафик. Эти проверки не должны выполнять дорогой полный бизнес-сценарий.

Stateless-экземпляры масштабируются горизонтально. Файлы, сессии и очередь задач выносятся в предназначенные для этого сервисы.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Неверный `trust proxy` позволяет подделать протокол или IP и нарушает secure cookie и rate limiting.

### Применение

Отделите чистое преобразование по теме «Пример использования» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 52. Настройка параметров компрессии

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Обработка только happy path оставляет приложение без определённого поведения при сбое.

### Применение

Опишите для темы «Настройка параметров компрессии» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 53. Проверка работы компрессии в Express-приложении

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Соберите минимальный пример по теме «Проверка работы компрессии в Express-приложении» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 54. Шаг 1: Установка зависимостей

Конфигурация приходит из окружения, но строки из `process.env` всё равно требуют преобразования и проверки. Ошибка обязательной настройки должна останавливать приложение при старте.

Современный Node.js предоставляет watch mode, env-file, test runner и `util.parseArgs`; небольшому проекту часто не нужны отдельные пакеты для этих задач.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Пакет с именем встроенного модуля, например `fs` или `path`, не заменяет API Node.js и обычно вообще не должен устанавливаться.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Установка зависимостей» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 55. Шаг 2: Создание Express-приложения

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Опишите для темы «Шаг 2: Создание Express-приложения» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 56. Шаг 3: Создание статических файлов

Путь файловой системы и URL — разные модели. Путь строят через `node:path`, а ресурс рядом с ESM-модулем находят относительно `import.meta.url`.

FileHandle надо закрывать детерминированно. Высокоуровневые функции `readFile` и `writeFile` управляют handle сами, а низкоуровневый `open` передаёт ответственность вызывающему коду.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Path traversal позволяет выйти из разрешённого каталога через `..`, абсолютный путь или особенности платформы.

### Применение

Соберите минимальный пример по теме «Шаг 3: Создание статических файлов» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 57. Шаг 4: Запуск приложения

Отмена является частью контракта операции. AbortSignal надо передавать вниз по слоям, чтобы остановить HTTP-запрос, таймер или другую поддерживаемую работу, когда результат больше не нужен.

После завершения синхронного фрагмента runtime освобождает стек, обрабатывает microtasks и продолжает фазы event loop. Поэтому порядок Promise, timer и `setImmediate` нельзя объяснять только временем задержки.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Забытый `await` превращает ожидаемую ошибку в необработанный rejection и может завершить запрос раньше фактической операции.

### Применение

Отделите чистое преобразование по теме «Шаг 4: Запуск приложения» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 58. Шаг 5: Проверка компрессии

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Опишите для темы «Шаг 5: Проверка компрессии» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 59. Для проверки компрессии данных в Express-приложении через Postman следуйте этим шагам:

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Соберите минимальный пример по теме «Для проверки компрессии данных в Express-приложении через Postman следуйте этим шагам:» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 60. Шаг 1: Отправка запроса

Современное решение должно быть проверяемым, наблюдаемым и безопасным по умолчанию. Удобство демонстрации не должно скрывать production-ограничения.

Ресурс открывается как можно позже и закрывается детерминированно. Таймаут и отмена проходят через все поддерживающие их слои.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Неявная зависимость усложняет тестирование и приводит к различиям между локальным и production-окружением.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Отправка запроса» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 61. Шаг 2: Анализ ответа

Асинхронность описывает возможность продолжить полезную работу во время ожидания, а параллельность — одновременное исполнение на нескольких вычислительных ресурсах. В Node.js эти понятия связаны, но не тождественны.

Worker Thread имеет собственный JavaScript isolate. Передача больших данных может стоить дорого, поэтому оценивают сериализацию, Transferable objects и размер пула.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Забытый `await` превращает ожидаемую ошибку в необработанный rejection и может завершить запрос раньше фактической операции.

### Применение

Опишите для темы «Шаг 2: Анализ ответа» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 62. Шаг 3: Дополнительные проверки

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Соберите минимальный пример по теме «Шаг 3: Дополнительные проверки» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 63. Пример

Liveness отвечает, жив ли процесс; readiness — способен ли экземпляр принимать трафик. Эти проверки не должны выполнять дорогой полный бизнес-сценарий.

Контейнер запускается не от root, получает секреты во время исполнения и содержит только необходимые production-зависимости.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Жёстко записанный секрет попадает в image layers и остаётся там после удаления из последнего слоя.

### Применение

Отделите чистое преобразование по теме «Пример» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 64. Практическое задание: Настройка компрессии для уменьшения времени загрузки страниц.

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Входные данные нормализуются на границе, бизнес-правила выполняются отдельно, а побочные эффекты получают явного владельца.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Опишите для темы «Практическое задание: Настройка компрессии для уменьшения времени загрузки страниц.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 65. Практическое задание: Настройка компрессии для уменьшения времени загрузки страниц

Promise представляет будущий результат, а `async/await` делает последовательность операций читаемой. Ошибка rejected Promise должна иметь владельца: обработчик запроса, задача очереди или верхнеуровневая политика завершения.

После завершения синхронного фрагмента runtime освобождает стек, обрабатывает microtasks и продолжает фазы event loop. Поэтому порядок Promise, timer и `setImmediate` нельзя объяснять только временем задержки.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Неограниченный `Promise.all` создаёт всплеск соединений и памяти. Конкурентность должна иметь лимит и политику отказа.

### Применение

Соберите минимальный пример по теме «Практическое задание: Настройка компрессии для уменьшения времени загрузки страниц» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 66. Шаг 1: Установка мидлвара compression

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Установка мидлвара compression» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 67. Шаг 2: Подключение мидлвара compression в вашем Express-приложении

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Опишите для темы «Шаг 2: Подключение мидлвара compression в вашем Express-приложении» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 68. Шаг 3: Тестирование компрессии

Встроенный `node:test` поддерживает suites, hooks, mocking, watch mode и coverage. Для большинства учебных примеров отдельный test framework не обязателен.

Dependency injection часов, random и fetch делает тест детерминированным без глобальной подмены runtime.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Чрезмерный mock проверяет собственные ожидания теста, а не совместимость с реальной системой.

### Применение

Соберите минимальный пример по теме «Шаг 3: Тестирование компрессии» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 69. Мониторинг и анализ производительности с помощью инструментов как PM2

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Отделите чистое преобразование по теме «Мониторинг и анализ производительности с помощью инструментов как PM2» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 70. Установка PM2

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Опишите для темы «Установка PM2» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 71. Настройка PM2 для мониторинга приложения Express

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Соберите минимальный пример по теме «Настройка PM2 для мониторинга приложения Express» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 72. Использование ключевых функций PM2 для мониторинга и анализа

Современное решение должно быть проверяемым, наблюдаемым и безопасным по умолчанию. Удобство демонстрации не должно скрывать production-ограничения.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Отделите чистое преобразование по теме «Использование ключевых функций PM2 для мониторинга и анализа» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 73. Мониторинг в реальном времени

WebSocket создаёт постоянный двунаправленный канал после HTTP Upgrade. Протокол приложения всё равно должен определить типы сообщений, идентификаторы, ошибки и версионирование.

Heartbeat обнаруживает полумёртвые соединения. Сервер отправляет ping, отмечает pong и завершает клиента, который не ответил в ожидаемом окне.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Доверие к userId из payload позволяет действовать от имени другого пользователя; идентичность берут из проверенного контекста соединения.

### Применение

Опишите для темы «Мониторинг в реальном времени» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 74. Список запущенных приложений

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Логирование всего тела запроса увеличивает стоимость, шум и риск утечки чувствительных данных.

### Применение

Соберите минимальный пример по теме «Список запущенных приложений» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 75. Просмотр логов

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Логирование всего тела запроса увеличивает стоимость, шум и риск утечки чувствительных данных.

### Применение

Отделите чистое преобразование по теме «Просмотр логов» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 76. Автоматический перезапуск при изменении файлов

Для прикладного асинхронного кода используется `node:fs/promises`. Синхронные функции допустимы в коротком CLI или на этапе запуска, но не в горячем пути HTTP-запроса.

Метаданные `stat` могут измениться сразу после проверки. Для безопасности нельзя строить решение только на последовательности check-then-use.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Наблюдение за каталогом зависит от ОС и не является гарантированной очередью бизнес-событий.

### Применение

Опишите для темы «Автоматический перезапуск при изменении файлов» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 77. Конфигурация в файле ecosystem.config.js

Путь файловой системы и URL — разные модели. Путь строят через `node:path`, а ресурс рядом с ESM-модулем находят относительно `import.meta.url`.

Метаданные `stat` могут измениться сразу после проверки. Для безопасности нельзя строить решение только на последовательности check-then-use.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Наблюдение за каталогом зависит от ОС и не является гарантированной очередью бизнес-событий.

### Применение

Соберите минимальный пример по теме «Конфигурация в файле ecosystem.config.js» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 78. Пример использования PM2 для запуска, мониторинга и масштабирования простого Express-приложения

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Логирование всего тела запроса увеличивает стоимость, шум и риск утечки чувствительных данных.

### Применение

Отделите чистое преобразование по теме «Пример использования PM2 для запуска, мониторинга и масштабирования простого Express-приложения» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 79. Шаг 1: Создание Express-приложения

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Опишите для темы «Шаг 1: Создание Express-приложения» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 80. Шаг 2: Запуск приложения с PM2

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Обработка только happy path оставляет приложение без определённого поведения при сбое.

### Применение

Соберите минимальный пример по теме «Шаг 2: Запуск приложения с PM2» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 81. Шаг 3: Мониторинг приложения

Отмена является частью контракта операции. AbortSignal надо передавать вниз по слоям, чтобы остановить HTTP-запрос, таймер или другую поддерживаемую работу, когда результат больше не нужен.

Worker Thread имеет собственный JavaScript isolate. Передача больших данных может стоить дорого, поэтому оценивают сериализацию, Transferable objects и размер пула.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Продолжение работы после `uncaughtException` опасно: состояние процесса может быть повреждено. Процесс корректно завершают и доверяют перезапуск supervisor-платформе.

### Применение

Отделите чистое преобразование по теме «Шаг 3: Мониторинг приложения» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 82. Шаг 4: Масштабирование приложения

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Опишите для темы «Шаг 4: Масштабирование приложения» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 83. Шаг 5: Просмотр логов

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Event loop delay показывает, насколько основной поток не мог продолжить работу. CPU profile отвечает, какие функции занимали процессор.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Соберите минимальный пример по теме «Шаг 5: Просмотр логов» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 84. Шаг 6: Управление процессами

Production-проект должен использовать поддерживаемую LTS-ветку. Для курса выбрана Node.js 24: она получает исправления безопасности, а её API можно сверять с документацией конкретной major-версии.

JavaScript выполняется до ближайшей точки возврата управления. Затем runtime может обработать microtasks, перейти к следующей фазе event loop и вызвать готовые callbacks.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Главный риск — заблокировать event loop синхронной операцией или большим вычислением и тем самым задержать все подключения процесса.

### Применение

Отделите чистое преобразование по теме «Шаг 6: Управление процессами» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 85. Шаг 7: Автоматический запуск приложений при старте системы

Асинхронность описывает возможность продолжить полезную работу во время ожидания, а параллельность — одновременное исполнение на нескольких вычислительных ресурсах. В Node.js эти понятия связаны, но не тождественны.

Callback API часто использует соглашение error-first. При переводе в Promise важно сохранить ошибку, не вызвать resolve дважды и корректно освободить ресурсы.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Забытый `await` превращает ожидаемую ошибку в необработанный rejection и может завершить запрос раньше фактической операции.

### Применение

Опишите для темы «Шаг 7: Автоматический запуск приложений при старте системы» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 86. Шаг 8: Сохранение списка запущенных приложений

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Соберите минимальный пример по теме «Шаг 8: Сохранение списка запущенных приложений» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 87. Интерпретация данных мониторинга и логов для выявления узких мест.

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Compression уменьшает сетевой объём ценой CPU. Статику часто эффективнее сжимает reverse proxy или CDN заранее.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Отделите чистое преобразование по теме «Интерпретация данных мониторинга и логов для выявления узких мест.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 88. 1. Анализ данных мониторинга

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Structured logs, metrics, traces и graceful shutdown

Неявная зависимость усложняет тестирование и приводит к различиям между локальным и production-окружением.

### Применение

Опишите для темы «1. Анализ данных мониторинга» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 89. 2. Интерпретация логов

Кэш хранит результат вычисления ближе к потребителю, но требует явных key, TTL, лимита памяти и стратегии invalidation.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: Профилирование event loop и CPU

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Соберите минимальный пример по теме «2. Интерпретация логов» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 90. 3. Выявление узких мест

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: Кэширование и инвалидация

Кэш без invalidation возвращает устаревшие данные, а без ограничения превращается в утечку памяти.

### Применение

Отделите чистое преобразование по теме «3. Выявление узких мест» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 91. 4. Решение проблемы

Production-сборка должна быть воспроизводимой: поддерживаемая Node.js, lock-файл, минимальный артефакт и известная команда запуска.

При SIGTERM приложение снимает readiness, перестаёт принимать новые соединения, ждёт активные запросы в пределах deadline и закрывает ресурсы.

В контексте этого урока раздел связан с задачей: Compression, ETag и reverse proxy

Deploy без rollback и совместимости миграции превращает обычную ошибку версии в длительный простой.

### Применение

Опишите для темы «4. Решение проблемы» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## Цельный пример урока

Ниже приведён актуальный минимальный пример. Он размещён прямо в материале, чтобы теория и код не расходились по отдельным каталогам.

### `app.mjs`

```js
import express from 'express';
import compression from 'compression';
import pinoHttp from 'pino-http';
import { monitorEventLoopDelay } from 'node:perf_hooks';
const lag = monitorEventLoopDelay({ resolution: 20 });
lag.enable();
const app = express();
app.use(pinoHttp());
app.use(compression());
app.get('/health', (req, res) => res.json({ status: 'ok' }));
app.get('/metrics', (req, res) => res.json({
  eventLoopP99Ms: Number(lag.percentile(99) / 1e6).toFixed(2),
  memory: process.memoryUsage(),
}));
app.listen(3000);
```

### `package.json`

```json
{
  "name": "lesson-6-modern-example",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node app.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.1.0",
    "pino-http": "^11.0.0"
  }
}
```

## Итоговая работа

Соберите небольшой проект по теме урока. README проекта должен содержать назначение, требования, команды запуска, контракт входа и выхода, перечень ожидаемых ошибок и описание решения для graceful shutdown.

Минимальная проверка:

- успешный сценарий;
- неверный ввод;
- отказ внешней зависимости;
- timeout или отмена;
- повтор операции;
- завершение процесса;
- отсутствие секретов в логах и репозитории.

## Официальные источники

- [Express performance](https://expressjs.com/en/advanced/best-practice-performance.html)
- [Node diagnostics](https://nodejs.org/en/learn/diagnostics)
- [Документация Node.js 24](https://nodejs.org/docs/latest-v24.x/api/)

- [Поддерживаемые версии Node.js](https://nodejs.org/en/about/previous-releases)
- [Node.js Learn](https://nodejs.org/en/learn)

## Критерии завершения урока

- Вы объясняете механизм без привязки к одному примеру.
- Вы понимаете границы встроенного API и внешней библиотеки.
- Вы умеете обработать ошибку, timeout и отмену.
- Вы можете проверить решение автоматическим тестом.
- Вы знаете, где найти статус и документацию использованного API.
