# 6.1. Express 5 с нуля: приложение и middleware

> Актуальная версия курса: Node.js 24 LTS, ECMAScript Modules и Express 5.

## О чём урок

Этот урок — самостоятельная глава курса. Материал построен от модели выполнения к проектным решениям: сначала разбираем, что делает платформа, затем пишем код, определяем отказоустойчивость и закрепляем результат практикой.

Старые примеры и устаревшие версии библиотек в эту редакцию не перенесены. Все объяснения сформулированы заново для современного Node.js.

## Как работать с материалом

1. Прочитайте раздел и сформулируйте его основную мысль своими словами.
2. До запуска примера предскажите результат и возможную ошибку.
3. Наберите код вручную, не вставляя его целиком.
4. Измените одно условие и объясните новое поведение.
5. Выполните мини-практику и ответьте на вопросы.
6. Сверьте нестабильный API с документацией Node.js 24.

## Требования

```bash
node --version
npm --version
```

Используйте Node.js 24.x. Новый код пишется в формате ESM. Встроенные модули импортируются с префиксом `node:`.

## 1. Введение в Express Framework

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: Express как последовательность middleware

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Опишите для темы «Введение в Express Framework» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 2. Обзор Express: что это такое, зачем он нужен

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: app.use, методы маршрутов и Router

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Соберите минимальный пример по теме «Обзор Express: что это такое, зачем он нужен» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 3. Что такое Express?

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: req, res, next и завершение ответа

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Отделите чистое преобразование по теме «Что такое Express?» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 4. Зачем нужен Express?

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: JSON body, 404 и error middleware

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Опишите для темы «Зачем нужен Express?» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 5. Различия между Express и встроенным веб-модулем Node.js

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Express как последовательность middleware

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Соберите минимальный пример по теме «Различия между Express и встроенным веб-модулем Node.js» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 6. Встроенный HTTP-модуль Node.js

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: app.use, методы маршрутов и Router

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Отделите чистое преобразование по теме «Встроенный HTTP-модуль Node.js» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 7. Express.js

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: req, res, next и завершение ответа

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Опишите для темы «Express.js» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 8. Установка Express и создание базового сервера

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: JSON body, 404 и error middleware

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Соберите минимальный пример по теме «Установка Express и создание базового сервера» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 9. Создание нового проекта

Express 5 автоматически передаёт rejected Promise из async-handler в error middleware. Ошибки callback API всё ещё требуют явного `next(error)`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: Express как последовательность middleware

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Отделите чистое преобразование по теме «Создание нового проекта» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 10. Установка Express

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: app.use, методы маршрутов и Router

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Опишите для темы «Установка Express» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 11. Создание базового сервера

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: req, res, next и завершение ответа

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Соберите минимальный пример по теме «Создание базового сервера» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 12. Запуск сервера

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: JSON body, 404 и error middleware

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Отделите чистое преобразование по теме «Запуск сервера» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 13. Промежуточное ПО

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Express как последовательность middleware

Передача клиенту `error.stack` или сообщения драйвера раскрывает внутреннее устройство приложения.

### Применение

Опишите для темы «Промежуточное ПО» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 14. Как работает Middleware

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: app.use, методы маршрутов и Router

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Соберите минимальный пример по теме «Как работает Middleware» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 15. Примеры использования Middleware

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: req, res, next и завершение ответа

Доверие к proxy-заголовкам без списка доверенных прокси позволяет подделать IP, protocol или host.

### Применение

Отделите чистое преобразование по теме «Примеры использования Middleware» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 16. Логирование запросов

Оптимизация начинается с измерения latency, throughput, error rate и saturation. Среднее время недостаточно: p95 и p99 показывают медленный хвост.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: JSON body, 404 и error middleware

Высокая конкурентность без backpressure переносит очередь в память и ухудшает tail latency.

### Применение

Опишите для темы «Логирование запросов» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 17. Аутентификация пользователя

Пароль хранится как медленный salted hash с актуальными параметрами. При успешном входе старый hash можно постепенно обновить до новой политики.

Object-level authorization выполняется после загрузки ресурса и сравнивает actor с owner, membership или политикой роли.

В контексте этого урока раздел связан с задачей: Express как последовательность middleware

Разные ответы и время для существующего и несуществующего пользователя помогают перечислять учётные записи.

### Применение

Соберите минимальный пример по теме «Аутентификация пользователя» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 18. Обработка статических файлов

Запись важного файла проектируют с учётом сбоя посередине: временный файл, проверка результата и атомарный rename уменьшают вероятность повреждения.

Пользовательский относительный путь нормализуют, разрешают внутри доверенного корня и повторно проверяют итоговую границу.

В контексте этого урока раздел связан с задачей: app.use, методы маршрутов и Router

Параллельная запись в один файл без координации приводит к потерянным обновлениям.

### Применение

Отделите чистое преобразование по теме «Обработка статических файлов» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 19. Обработка ошибок

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

Порядок `app.use` определяет поведение. JSON parser регистрируют до маршрута, 404-handler после всех маршрутов, а error middleware с четырьмя аргументами — последним.

В контексте этого урока раздел связан с задачей: req, res, next и завершение ответа

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Опишите для темы «Обработка ошибок» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 20. Парсинг тела запроса

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: JSON body, 404 и error middleware

Два вызова отправки ответа приводят к `headers already sent`; обычно это ошибка управления потоком.

### Применение

Соберите минимальный пример по теме «Парсинг тела запроса» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 21. Создание собственного Middleware

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

`node:http` создаёт request listener. Express использует тот же контракт и добавляет pipeline middleware, Router и методы формирования ответа.

В контексте этого урока раздел связан с задачей: Express как последовательность middleware

Два вызова отправки ответа приводят к `headers already sent`; обычно это ошибка управления потоком.

### Применение

Отделите чистое преобразование по теме «Создание собственного Middleware» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 22. Основы маршрутизации в Express

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: app.use, методы маршрутов и Router

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Опишите для темы «Основы маршрутизации в Express» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 23. Основные концепции маршрутизации

Входящий body является потоком и не имеет встроенной гарантии размера. До парсинга задают лимит, проверяют Content-Type и определяют поведение при преждевременном разрыве соединения.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: req, res, next и завершение ответа

Два вызова отправки ответа приводят к `headers already sent`; обычно это ошибка управления потоком.

### Применение

Соберите минимальный пример по теме «Основные концепции маршрутизации» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 24. Создание базового маршрута

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: JSON body, 404 и error middleware

Два вызова отправки ответа приводят к `headers already sent`; обычно это ошибка управления потоком.

### Применение

Отделите чистое преобразование по теме «Создание базового маршрута» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 25. Использование параметров маршрута

HTTP-запрос состоит из метода, target, заголовков и необязательного тела; ответ — из статуса, заголовков и тела. Сервер должен завершить каждую ветку ровно одним ответом.

Keep-alive переиспользует TCP-соединение, но сервер всё равно должен ограничивать таймауты заголовков, запроса и простоя.

В контексте этого урока раздел связан с задачей: Express как последовательность middleware

Два вызова отправки ответа приводят к `headers already sent`; обычно это ошибка управления потоком.

### Применение

Опишите для темы «Использование параметров маршрута» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 26. Работа с Query String

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: app.use, методы маршрутов и Router

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Соберите минимальный пример по теме «Работа с Query String» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 27. Использование Middleware

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

Keep-alive переиспользует TCP-соединение, но сервер всё равно должен ограничивать таймауты заголовков, запроса и простоя.

В контексте этого урока раздел связан с задачей: req, res, next и завершение ответа

Два вызова отправки ответа приводят к `headers already sent`; обычно это ошибка управления потоком.

### Применение

Отделите чистое преобразование по теме «Использование Middleware» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## Цельный пример урока

Ниже приведён актуальный минимальный пример. Он размещён прямо в материале, чтобы теория и код не расходились по отдельным каталогам.

### `app.mjs`

```js
import express from 'express';
export const app = express();
app.use(express.json({ limit: '32kb' }));
app.use((req, res, next) => {
  res.set('x-request-id', crypto.randomUUID());
  next();
});
app.get('/health', (req, res) => res.json({ status: 'ok' }));
app.use((req, res) => res.status(404).json({ title: 'Not Found', status: 404 }));
app.use((error, req, res, next) => {
  if (res.headersSent) return next(error);
  res.status(500).json({ title: 'Internal Server Error', status: 500 });
});
```

### `app.test.mjs`

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { once } from 'node:events';
import { app } from './app.mjs';
test('health endpoint and 404 contract', async (t) => {
  const server = app.listen(0);
  t.after(() => server.close());
  await once(server, 'listening');
  const base = `http://127.0.0.1:${server.address().port}`;
  const health = await fetch(`${base}/health`);
  assert.deepEqual(await health.json(), { status: 'ok' });
  assert.ok(health.headers.get('x-request-id'));
  assert.equal((await fetch(`${base}/missing`)).status, 404);
});
```

### `package.json`

```json
{
  "name": "lesson-6-modern-example",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node app.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
```

### `server.mjs`

```js
import { app } from './app.mjs';
const server = app.listen(process.env.PORT ?? 3000, (error) => {
  if (error) throw error;
  console.log('API: http://localhost:3000');
});
process.on('SIGTERM', () => server.close());
```

## Итоговая работа

Соберите небольшой проект по теме урока. README проекта должен содержать назначение, требования, команды запуска, контракт входа и выхода, перечень ожидаемых ошибок и описание решения для graceful shutdown.

Минимальная проверка:

- успешный сценарий;
- неверный ввод;
- отказ внешней зависимости;
- timeout или отмена;
- повтор операции;
- завершение процесса;
- отсутствие секретов в логах и репозитории.

## Официальные источники

- [Express 5](https://expressjs.com/en/5x/api.html)
- [Using middleware](https://expressjs.com/en/5x/guide/using-middleware.html)
- [Документация Node.js 24](https://nodejs.org/docs/latest-v24.x/api/)

- [Поддерживаемые версии Node.js](https://nodejs.org/en/about/previous-releases)
- [Node.js Learn](https://nodejs.org/en/learn)

## Критерии завершения урока

- Вы объясняете механизм без привязки к одному примеру.
- Вы понимаете границы встроенного API и внешней библиотеки.
- Вы умеете обработать ошибку, timeout и отмену.
- Вы можете проверить решение автоматическим тестом.
- Вы знаете, где найти статус и документацию использованного API.
