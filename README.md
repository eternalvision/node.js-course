# 6.3. Представления, конфигурация и переменные среды

> Актуальная версия курса: Node.js 24 LTS, ECMAScript Modules и Express 5.

## О чём урок

Этот урок — самостоятельная глава курса. Материал построен от модели выполнения к проектным решениям: сначала разбираем, что делает платформа, затем пишем код, определяем отказоустойчивость и закрепляем результат практикой.

Старые примеры и устаревшие версии библиотек в эту редакцию не перенесены. Все объяснения сформулированы заново для современного Node.js.

## Как работать с материалом

1. Прочитайте раздел и сформулируйте его основную мысль своими словами.
2. До запуска примера предскажите результат и возможную ошибку.
3. Наберите код вручную, не вставляя его целиком.
4. Измените одно условие и объясните новое поведение.
5. Выполните мини-практику и ответьте на вопросы.
6. Сверьте нестабильный API с документацией Node.js 24.

## Требования

```bash
node --version
npm --version
```

Используйте Node.js 24.x. Новый код пишется в формате ESM. Встроенные модули импортируются с префиксом `node:`.

## 1. Шаблонизаторы и генерация динамического контента. Переменные среды

Server-side rendering формирует HTML на сервере. Шаблон получает подготовленную view model с минимальным набором полей, а не объект базы целиком.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Опишите для темы «Шаблонизаторы и генерация динамического контента. Переменные среды» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 2. Переменные среды

В CI используется чистая установка по lock-файлу. Глобальные инструменты не должны быть скрытым условием сборки: команда проекта запускается через npm script или `npx` с зафиксированной версией.

dependencies нужны приложению во время работы, devDependencies — для разработки и сборки, peerDependencies описывают совместимость с зависимостью потребителя.

В контексте этого урока раздел связан с задачей: Layouts и partials

Секрет в репозитории остаётся скомпрометированным даже после удаления из последнего коммита; его необходимо отозвать и заменить.

### Применение

Соберите минимальный пример по теме «Переменные среды» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 3. Введение в шаблонизаторы

Layout задаёт общий каркас, partial переиспользует небольшой фрагмент, helper инкапсулирует простое отображение. Бизнес-логику в шаблон не переносят.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Отделите чистое преобразование по теме «Введение в шаблонизаторы» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 4. Обзор темы

Node.js — это среда выполнения JavaScript, а не язык и не веб-фреймворк. V8 компилирует и исполняет JavaScript, Node добавляет системные API, а libuv участвует в организации цикла событий и части фоновой работы.

I/O-конкурентность позволяет обслуживать много ожидающих операций, но CPU-цикл всё равно занимает основной поток. Для вычислений применяют Worker Threads, очередь задач или отдельный процесс.

В контексте этого урока раздел связан с задачей: Node --env-file и двенадцатифакторное приложение

Главный риск — заблокировать event loop синхронной операцией или большим вычислением и тем самым задержать все подключения процесса.

### Применение

Опишите для темы «Обзор темы» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 5. Что такое шаблонизаторы и зачем они нужны в веб-разработке.

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Соберите минимальный пример по теме «Что такое шаблонизаторы и зачем они нужны в веб-разработке.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 6. Зачем нужны шаблонизаторы?

Layout задаёт общий каркас, partial переиспользует небольшой фрагмент, helper инкапсулирует простое отображение. Бизнес-логику в шаблон не переносят.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: Layouts и partials

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Отделите чистое преобразование по теме «Зачем нужны шаблонизаторы?» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 7. Популярные шаблонизаторы для Express

Server-side rendering формирует HTML на сервере. Шаблон получает подготовленную view model с минимальным набором полей, а не объект базы целиком.

Кэширование скомпилированных шаблонов полезно в production, но данные конкретного пользователя не должны попадать в общий cache key.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Опишите для темы «Популярные шаблонизаторы для Express» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 8. Установка и настройка шаблонизатора

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

Кэширование скомпилированных шаблонов полезно в production, но данные конкретного пользователя не должны попадать в общий cache key.

В контексте этого урока раздел связан с задачей: Node --env-file и двенадцатифакторное приложение

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Соберите минимальный пример по теме «Установка и настройка шаблонизатора» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 9. Демонстрация на примере Handlebars: установка через npm, настройка в Express.

Layout задаёт общий каркас, partial переиспользует небольшой фрагмент, helper инкапсулирует простое отображение. Бизнес-логику в шаблон не переносят.

Кэширование скомпилированных шаблонов полезно в production, но данные конкретного пользователя не должны попадать в общий cache key.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Отделите чистое преобразование по теме «Демонстрация на примере Handlebars: установка через npm, настройка в Express.» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 10. Шаг 1: Установка Handlebars

Server-side rendering формирует HTML на сервере. Шаблон получает подготовленную view model с минимальным набором полей, а не объект базы целиком.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: Layouts и partials

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Опишите для темы «Шаг 1: Установка Handlebars» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 11. Шаг 2: Настройка Express для использования Handlebars

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Соберите минимальный пример по теме «Шаг 2: Настройка Express для использования Handlebars» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 12. Шаг 3: Создание базового шаблона страницы

Layout задаёт общий каркас, partial переиспользует небольшой фрагмент, helper инкапсулирует простое отображение. Бизнес-логику в шаблон не переносят.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: Node --env-file и двенадцатифакторное приложение

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Отделите чистое преобразование по теме «Шаг 3: Создание базового шаблона страницы» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 13. Шаг 4: Рендеринг страницы с использованием шаблона

Server-side rendering формирует HTML на сервере. Шаблон получает подготовленную view model с минимальным набором полей, а не объект базы целиком.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Опишите для темы «Шаг 4: Рендеринг страницы с использованием шаблона» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 14. Основы работы с Handlebars

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: Layouts и partials

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Соберите минимальный пример по теме «Основы работы с Handlebars» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 15. Вставка переменных

Современное решение должно быть проверяемым, наблюдаемым и безопасным по умолчанию. Удобство демонстрации не должно скрывать production-ограничения.

Ресурс открывается как можно позже и закрывается детерминированно. Таймаут и отмена проходят через все поддерживающие их слои.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Отделите чистое преобразование по теме «Вставка переменных» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 16. Использование условий

Node.js — это среда выполнения JavaScript, а не язык и не веб-фреймворк. V8 компилирует и исполняет JavaScript, Node добавляет системные API, а libuv участвует в организации цикла событий и части фоновой работы.

Состояние процесса включает аргументы запуска, переменные среды, открытые handles, память и код завершения. Эти части надо проектировать так же внимательно, как HTTP-маршруты.

В контексте этого урока раздел связан с задачей: Node --env-file и двенадцатифакторное приложение

Глобальное изменяемое состояние усложняет тестирование, создаёт неявные зависимости и расходится между несколькими экземплярами приложения.

### Применение

Опишите для темы «Использование условий» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 17. Использование циклов

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

Кэширование скомпилированных шаблонов полезно в production, но данные конкретного пользователя не должны попадать в общий cache key.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Соберите минимальный пример по теме «Использование циклов» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 18. Вложенные объекты

Layout задаёт общий каркас, partial переиспользует небольшой фрагмент, helper инкапсулирует простое отображение. Бизнес-логику в шаблон не переносят.

Кэширование скомпилированных шаблонов полезно в production, но данные конкретного пользователя не должны попадать в общий cache key.

В контексте этого урока раздел связан с задачей: Layouts и partials

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Отделите чистое преобразование по теме «Вложенные объекты» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 19. Помощники (Helpers)

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Входные данные нормализуются на границе, бизнес-правила выполняются отдельно, а побочные эффекты получают явного владельца.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Обработка только happy path оставляет приложение без определённого поведения при сбое.

### Применение

Опишите для темы «Помощники (Helpers)» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 20. Простой сервер на Express, который демонстрирует использование шаблонизатора Handlebars для отображения динамического контента.

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: Node --env-file и двенадцатифакторное приложение

Смешение API JSON и HTML-ответов в одном handler усложняет negotiation и обработку ошибок.

### Применение

Соберите минимальный пример по теме «Простой сервер на Express, который демонстрирует использование шаблонизатора Handlebars для отображения динамического контента.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 21. Шаг 1: Установка необходимых пакетов

Конфигурация приходит из окружения, но строки из `process.env` всё равно требуют преобразования и проверки. Ошибка обязательной настройки должна останавливать приложение при старте.

dependencies нужны приложению во время работы, devDependencies — для разработки и сборки, peerDependencies описывают совместимость с зависимостью потребителя.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Пакет с именем встроенного модуля, например `fs` или `path`, не заменяет API Node.js и обычно вообще не должен устанавливаться.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Установка необходимых пакетов» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 22. Шаг 2: Настройка сервера Express

Express 5 представляет приложение как упорядоченную цепочку middleware. Каждый участник либо завершает ответ, либо вызывает `next`, либо передаёт ошибку.

`req.params`, `req.query` и `req.body` являются недоверенными данными. После парсинга выполняют schema validation и передают вниз нормализованный DTO.

В контексте этого урока раздел связан с задачей: Layouts и partials

Большой controller, одновременно работающий с HTTP, базой и бизнес-правилами, становится плохо тестируемым и связанным.

### Применение

Опишите для темы «Шаг 2: Настройка сервера Express» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 23. Шаг 3: Создание шаблона Handlebars

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Смешение API JSON и HTML-ответов в одном handler усложняет negotiation и обработку ошибок.

### Применение

Соберите минимальный пример по теме «Шаг 3: Создание шаблона Handlebars» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 24. Шаг 4: Запуск сервера

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: Node --env-file и двенадцатифакторное приложение

Два вызова отправки ответа приводят к `headers already sent`; обычно это ошибка управления потоком.

### Применение

Отделите чистое преобразование по теме «Шаг 4: Запуск сервера» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 25. Дополнения для работы с данными

Server-side rendering формирует HTML на сервере. Шаблон получает подготовленную view model с минимальным набором полей, а не объект базы целиком.

Кэширование скомпилированных шаблонов полезно в production, но данные конкретного пользователя не должны попадать в общий cache key.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Смешение API JSON и HTML-ответов в одном handler усложняет negotiation и обработку ошибок.

### Применение

Опишите для темы «Дополнения для работы с данными» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 26. Шаг 1: Создание JSON-файла с данными

Путь файловой системы и URL — разные модели. Путь строят через `node:path`, а ресурс рядом с ESM-модулем находят относительно `import.meta.url`.

Пользовательский относительный путь нормализуют, разрешают внутри доверенного корня и повторно проверяют итоговую границу.

В контексте этого урока раздел связан с задачей: Layouts и partials

Наблюдение за каталогом зависит от ОС и не является гарантированной очередью бизнес-событий.

### Применение

Соберите минимальный пример по теме «Шаг 1: Создание JSON-файла с данными» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 27. Шаг 2: Чтение данных из JSON-файла

Запись важного файла проектируют с учётом сбоя посередине: временный файл, проверка результата и атомарный rename уменьшают вероятность повреждения.

Пользовательский относительный путь нормализуют, разрешают внутри доверенного корня и повторно проверяют итоговую границу.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Наблюдение за каталогом зависит от ОС и не является гарантированной очередью бизнес-событий.

### Применение

Отделите чистое преобразование по теме «Шаг 2: Чтение данных из JSON-файла» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 28. Шаг 3: Модификация шаблона для отображения ToDo

Server-side rendering формирует HTML на сервере. Шаблон получает подготовленную view model с минимальным набором полей, а не объект базы целиком.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: Node --env-file и двенадцатифакторное приложение

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Опишите для темы «Шаг 3: Модификация шаблона для отображения ToDo» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 29. Шаг 4: Тестирование через Postman

Встроенный `node:test` поддерживает suites, hooks, mocking, watch mode и coverage. Для большинства учебных примеров отдельный test framework не обязателен.

Unit-тест изолирует чистую логику, integration-тест проверяет реальную границу, end-to-end проходит через публичный интерфейс.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Тест с реальным временем, случайностью или интернетом становится flaky и теряет доверие команды.

### Применение

Соберите минимальный пример по теме «Шаг 4: Тестирование через Postman» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 30. Генерация динамического контента

Layout задаёт общий каркас, partial переиспользует небольшой фрагмент, helper инкапсулирует простое отображение. Бизнес-логику в шаблон не переносят.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: Layouts и partials

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Отделите чистое преобразование по теме «Генерация динамического контента» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 31. Работа с данными в шаблонах

Server-side rendering формирует HTML на сервере. Шаблон получает подготовленную view model с минимальным набором полей, а не объект базы целиком.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Опишите для темы «Работа с данными в шаблонах» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 32. Шаг 2: Подключение к MongoDB

MongoDB допускает гибкую форму документов, но production-приложению всё равно нужен явный контракт и validation. Mongoose schema помогает на уровне приложения, а критичные правила полезно закреплять и в базе.

ObjectId является идентификатором, но входную строку всё равно проверяют до запроса и корректно преобразуют ошибку в клиентский статус.

В контексте этого урока раздел связан с задачей: Node --env-file и двенадцатифакторное приложение

Неограниченный массив внутри документа приближает лимит размера и делает каждое обновление тяжелее.

### Применение

Соберите минимальный пример по теме «Шаг 2: Подключение к MongoDB» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 33. Шаг 3: Создание маршрута для отображения пользователей

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

Статус и заголовки отправляются до первого фрагмента тела. После этого изменить их нельзя; ошибка во время streaming требует отдельной политики.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Отделите чистое преобразование по теме «Шаг 3: Создание маршрута для отображения пользователей» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 34. Шаг 4: Создание шаблона Handlebars для отображения списка пользователей

Server-side rendering формирует HTML на сервере. Шаблон получает подготовленную view model с минимальным набором полей, а не объект базы целиком.

Кэширование скомпилированных шаблонов полезно в production, но данные конкретного пользователя не должны попадать в общий cache key.

В контексте этого урока раздел связан с задачей: Layouts и partials

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

### Применение

Опишите для темы «Шаг 4: Создание шаблона Handlebars для отображения списка пользователей» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 35. Дополним предыдущий пример, добавив полный серверный код, который включает создание базы данных, добавление, изменение и удаление записей пользователей в MongoDB с использованием Express и Mongoose.

Router группирует транспортный код одного ресурса. Controller разбирает HTTP-вход, вызывает use case и формирует ответ; бизнес-правила не должны зависеть от `req` и `res`.

Если `res.headersSent` уже истинен, custom error handler делегирует ошибку встроенному обработчику, потому что безопасно заменить ответ уже нельзя.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Middleware, который не отвечает и не вызывает `next`, оставляет запрос висеть.

### Применение

Соберите минимальный пример по теме «Дополним предыдущий пример, добавив полный серверный код, который включает создание базы данных, добавление, изменение и удаление записей пользователей в MongoDB с использованием Express и Mongoose.» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 36. Шаг 1: Полная настройка сервера

Маршрут сопоставляет метод и путь. Query параметры описывают представление коллекции, path параметры идентифицируют ресурс, а body передаёт новое состояние.

Keep-alive переиспользует TCP-соединение, но сервер всё равно должен ограничивать таймауты заголовков, запроса и простоя.

В контексте этого урока раздел связан с задачей: Node --env-file и двенадцатифакторное приложение

Отсутствие лимита body, таймаутов или ограничения заголовков открывает путь к исчерпанию памяти и соединений.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Полная настройка сервера» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 37. Шаг 2: Создание и использование HTML форм

Server-side rendering формирует HTML на сервере. Шаблон получает подготовленную view model с минимальным набором полей, а не объект базы целиком.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Опишите для темы «Шаг 2: Создание и использование HTML форм» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 38. Создание страницы с динамическим содержимым. Макетирование и частичные шаблоны

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: Layouts и partials

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Соберите минимальный пример по теме «Создание страницы с динамическим содержимым. Макетирование и частичные шаблоны» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 39. Шаг 1: Создание частичных шаблонов

Layout задаёт общий каркас, partial переиспользует небольшой фрагмент, helper инкапсулирует простое отображение. Бизнес-логику в шаблон не переносят.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Создание частичных шаблонов» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 40. Шаг 2: Регистрация частичных шаблонов

Server-side rendering формирует HTML на сервере. Шаблон получает подготовленную view model с минимальным набором полей, а не объект базы целиком.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: Node --env-file и двенадцатифакторное приложение

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Опишите для темы «Шаг 2: Регистрация частичных шаблонов» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 41. Шаг 3: Использование частичных шаблонов в основном макете

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Соберите минимальный пример по теме «Шаг 3: Использование частичных шаблонов в основном макете» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 42. Продвинутые техники шаблонизации

Layout задаёт общий каркас, partial переиспользует небольшой фрагмент, helper инкапсулирует простое отображение. Бизнес-логику в шаблон не переносят.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: Layouts и partials

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Отделите чистое преобразование по теме «Продвинутые техники шаблонизации» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 43. Шаг 1: Создание хелпера для форматирования даты

Тема рассматривается через публичный контракт, поток данных и ответственность каждого слоя. Такой подход позволяет перенести знание на другой проект, а не запомнить один фрагмент кода.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Опишите для темы «Шаг 1: Создание хелпера для форматирования даты» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 44. Шаг 2: Использование хелпера в шаблоне

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

Кэширование скомпилированных шаблонов полезно в production, но данные конкретного пользователя не должны попадать в общий cache key.

В контексте этого урока раздел связан с задачей: Node --env-file и двенадцатифакторное приложение

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Соберите минимальный пример по теме «Шаг 2: Использование хелпера в шаблоне» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 45. Шаг 3: Стилизация списка статей

Layout задаёт общий каркас, partial переиспользует небольшой фрагмент, helper инкапсулирует простое отображение. Бизнес-логику в шаблон не переносят.

Кэширование скомпилированных шаблонов полезно в production, но данные конкретного пользователя не должны попадать в общий cache key.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Передача секрета или внутреннего поля в locals может привести к утечке в HTML даже без явного отображения.

### Применение

Отделите чистое преобразование по теме «Шаг 3: Стилизация списка статей» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 46. Пример Express-приложения с использованием Handlebars, которое включает работу с MongoDB, определение модели, использование хелперов для форматирования данных, переиспользование компонентов через частичные шаблоны, а также промежуточное ПО (middleware). Этот пример будет представлять собой простое веб-приложение для публикации и просмотра статей.

Server-side rendering формирует HTML на сервере. Шаблон получает подготовленную view model с минимальным набором полей, а не объект базы целиком.

Express выбирает engine по настройке view engine, находит файл в views и передаёт locals в функцию рендера.

В контексте этого урока раздел связан с задачей: Layouts и partials

Смешение API JSON и HTML-ответов в одном handler усложняет negotiation и обработку ошибок.

### Применение

Опишите для темы «Пример Express-приложения с использованием Handlebars, которое включает работу с MongoDB, определение модели, использование хелперов для форматирования данных, переиспользование компонентов через частичные шаблоны, а также промежуточное ПО (middleware). Этот пример будет представлять собой простое веб-приложение для публикации и просмотра статей.» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 47. Шаг 1: Настройка проекта

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Входные данные нормализуются на границе, бизнес-правила выполняются отдельно, а побочные эффекты получают явного владельца.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Обработка только happy path оставляет приложение без определённого поведения при сбое.

### Применение

Соберите минимальный пример по теме «Шаг 1: Настройка проекта» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 48. Шаг 2: Определение модели статьи

Production-проект должен использовать поддерживаемую LTS-ветку. Для курса выбрана Node.js 24: она получает исправления безопасности, а её API можно сверять с документацией конкретной major-версии.

JavaScript выполняется до ближайшей точки возврата управления. Затем runtime может обработать microtasks, перейти к следующей фазе event loop и вызвать готовые callbacks.

В контексте этого урока раздел связан с задачей: Node --env-file и двенадцатифакторное приложение

Использование EOL-версии оставляет приложение без регулярных исправлений безопасности, даже если сам код продолжает запускаться.

### Применение

Отделите чистое преобразование по теме «Шаг 2: Определение модели статьи» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 49. Шаг 3: Настройка Express и Handlebars

Server-side rendering формирует HTML на сервере. Шаблон получает подготовленную view model с минимальным набором полей, а не объект базы целиком.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: Шаблонизатор и escaping

Смешение API JSON и HTML-ответов в одном handler усложняет negotiation и обработку ошибок.

### Применение

Опишите для темы «Шаг 3: Настройка Express и Handlebars» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 50. Шаг 4: Создание шаблонов

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

View model преобразует даты, статусы и разрешённые действия заранее, благодаря чему шаблон остаётся декларативным.

В контексте этого урока раздел связан с задачей: Layouts и partials

Смешение API JSON и HTML-ответов в одном handler усложняет negotiation и обработку ошибок.

### Применение

Соберите минимальный пример по теме «Шаг 4: Создание шаблонов» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 51. Шаг 5: Запуск приложения

Современное решение должно быть проверяемым, наблюдаемым и безопасным по умолчанию. Удобство демонстрации не должно скрывать production-ограничения.

Ресурс открывается как можно позже и закрывается детерминированно. Таймаут и отмена проходят через все поддерживающие их слои.

В контексте этого урока раздел связан с задачей: process.env и fail-fast validation

Обработка только happy path оставляет приложение без определённого поведения при сбое.

### Применение

Отделите чистое преобразование по теме «Шаг 5: Запуск приложения» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## Цельный пример урока

Ниже приведён актуальный минимальный пример. Он размещён прямо в материале, чтобы теория и код не расходились по отдельным каталогам.

### `app.mjs`

```js
import express from 'express';
import { config } from './config.mjs';
const app = express();
app.set('view engine', 'ejs');
app.get('/', (req, res) => res.render('home', { title: config.appName }));
app.listen(config.port);
```

### `config.mjs`

```js
const port = Number(process.env.PORT ?? 3000);
if (!Number.isInteger(port) || port < 1) throw new Error('PORT must be a positive integer');
export const config = Object.freeze({ port, appName: process.env.APP_NAME ?? 'Tasks' });
```

### `package.json`

```json
{
  "name": "lesson-6-modern-example",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node config.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.1.0"
  }
}
```

### `views/home.ejs`

```html
<!doctype html><html lang="ru"><meta charset="utf-8"><title><%= title %></title><h1><%= title %></h1></html>
```

## Дополнительный разбор 1: Шаблонизаторы и генерация динамического контента. Переменные среды

Обычный вывод шаблонизатора должен экранировать пользовательский текст. Raw HTML допустим только для доверенного или отдельно sanitised содержимого.

Кэширование скомпилированных шаблонов полезно в production, но данные конкретного пользователя не должны попадать в общий cache key.

Цель: Шаблонизатор и escaping

Отключение escaping для произвольного ввода создаёт stored или reflected XSS.

Проверьте решение на корректном вводе, отказе зависимости и повторном вызове.

Критерий готовности: поведение тестируется на Node.js 24, ошибка не раскрывает внутренние данные, а открытые ресурсы закрываются.

## Итоговая работа

Соберите небольшой проект по теме урока. README проекта должен содержать назначение, требования, команды запуска, контракт входа и выхода, перечень ожидаемых ошибок и описание решения для graceful shutdown.

Минимальная проверка:

- успешный сценарий;
- неверный ввод;
- отказ внешней зависимости;
- timeout или отмена;
- повтор операции;
- завершение процесса;
- отсутствие секретов в логах и репозитории.

## Официальные источники

- [Express template engines](https://expressjs.com/en/guide/using-template-engines.html)
- [Node CLI env-file](https://nodejs.org/docs/latest-v24.x/api/cli.html#--env-fileconfig)
- [Документация Node.js 24](https://nodejs.org/docs/latest-v24.x/api/)

- [Поддерживаемые версии Node.js](https://nodejs.org/en/about/previous-releases)
- [Node.js Learn](https://nodejs.org/en/learn)

## Критерии завершения урока

- Вы объясняете механизм без привязки к одному примеру.
- Вы понимаете границы встроенного API и внешней библиотеки.
- Вы умеете обработать ошибку, timeout и отмену.
- Вы можете проверить решение автоматическим тестом.
- Вы знаете, где найти статус и документацию использованного API.
