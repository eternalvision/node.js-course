# 7.3. Деплой Node.js: AWS Elastic Beanstalk и общий production checklist

> Актуальная версия курса: Node.js 24 LTS, ECMAScript Modules и Express 5.

## О чём урок

Этот урок — самостоятельная глава курса. Материал построен от модели выполнения к проектным решениям: сначала разбираем, что делает платформа, затем пишем код, определяем отказоустойчивость и закрепляем результат практикой.

Старые примеры и устаревшие версии библиотек в эту редакцию не перенесены. Все объяснения сформулированы заново для современного Node.js.

## Как работать с материалом

1. Прочитайте раздел и сформулируйте его основную мысль своими словами.
2. До запуска примера предскажите результат и возможную ошибку.
3. Наберите код вручную, не вставляя его целиком.
4. Измените одно условие и объясните новое поведение.
5. Выполните мини-практику и ответьте на вопросы.
6. Сверьте нестабильный API с документацией Node.js 24.

## Требования

```bash
node --version
npm --version
```

Используйте Node.js 24.x. Новый код пишется в формате ESM. Встроенные модули импортируются с префиксом `node:`.

## 1. Развертывание Node.js приложения на Amazon Elastic Beanstalk

Production-сборка должна быть воспроизводимой: поддерживаемая Node.js, lock-файл, минимальный артефакт и известная команда запуска.

При SIGTERM приложение снимает readiness, перестаёт принимать новые соединения, ждёт активные запросы в пределах deadline и закрывает ресурсы.

В контексте этого урока раздел связан с задачей: Артефакт сборки и engines

Жёстко записанный секрет попадает в image layers и остаётся там после удаления из последнего слоя.

### Применение

Опишите для темы «Развертывание Node.js приложения на Amazon Elastic Beanstalk» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 2. Регистрация и настройка прав пользователя. Получение ключей

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Входные данные нормализуются на границе, бизнес-правила выполняются отдельно, а побочные эффекты получают явного владельца.

В контексте этого урока раздел связан с задачей: Конфигурация окружения и IAM

Неявная зависимость усложняет тестирование и приводит к различиям между локальным и production-окружением.

### Применение

Соберите минимальный пример по теме «Регистрация и настройка прав пользователя. Получение ключей» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 3. Шаг 1: Регистрация в AWS

Liveness отвечает, жив ли процесс; readiness — способен ли экземпляр принимать трафик. Эти проверки не должны выполнять дорогой полный бизнес-сценарий.

При SIGTERM приложение снимает readiness, перестаёт принимать новые соединения, ждёт активные запросы в пределах deadline и закрывает ресурсы.

В контексте этого урока раздел связан с задачей: Health checks, logs и alarms

Жёстко записанный секрет попадает в image layers и остаётся там после удаления из последнего слоя.

### Применение

Отделите чистое преобразование по теме «Шаг 1: Регистрация в AWS» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 4. Шаг 2: Создание нового пользователя в IAM

Production-сборка должна быть воспроизводимой: поддерживаемая Node.js, lock-файл, минимальный артефакт и известная команда запуска.

Stateless-экземпляры масштабируются горизонтально. Файлы, сессии и очередь задач выносятся в предназначенные для этого сервисы.

В контексте этого урока раздел связан с задачей: Blue/green, rolling deployment и rollback

Жёстко записанный секрет попадает в image layers и остаётся там после удаления из последнего слоя.

### Применение

Опишите для темы «Шаг 2: Создание нового пользователя в IAM» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 5. Шаг 3: Получение ключей доступа

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Ресурс открывается как можно позже и закрывается детерминированно. Таймаут и отмена проходят через все поддерживающие их слои.

В контексте этого урока раздел связан с задачей: Артефакт сборки и engines

Неявная зависимость усложняет тестирование и приводит к различиям между локальным и production-окружением.

### Применение

Соберите минимальный пример по теме «Шаг 3: Получение ключей доступа» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 6. Шаг 4: Настройка AWS CLI

Liveness отвечает, жив ли процесс; readiness — способен ли экземпляр принимать трафик. Эти проверки не должны выполнять дорогой полный бизнес-сценарий.

Stateless-экземпляры масштабируются горизонтально. Файлы, сессии и очередь задач выносятся в предназначенные для этого сервисы.

В контексте этого урока раздел связан с задачей: Конфигурация окружения и IAM

Жёстко записанный секрет попадает в image layers и остаётся там после удаления из последнего слоя.

### Применение

Отделите чистое преобразование по теме «Шаг 4: Настройка AWS CLI» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 7. Шаг 5. Настройка прав

Production-сборка должна быть воспроизводимой: поддерживаемая Node.js, lock-файл, минимальный артефакт и известная команда запуска.

Контейнер запускается не от root, получает секреты во время исполнения и содержит только необходимые production-зависимости.

В контексте этого урока раздел связан с задачей: Health checks, logs и alarms

Жёстко записанный секрет попадает в image layers и остаётся там после удаления из последнего слоя.

### Применение

Опишите для темы «Шаг 5. Настройка прав» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 8. Подготовка и настройка среды

OpenAPI описывает операции, schemas, security и ошибки как машиночитаемый контракт. Swagger UI является представлением контракта, а не заменой самой спецификации.

Контейнер запускается не от root, получает секреты во время исполнения и содержит только необходимые production-зависимости.

В контексте этого урока раздел связан с задачей: Blue/green, rolling deployment и rollback

Жёстко записанный секрет попадает в image layers и остаётся там после удаления из последнего слоя.

### Применение

Соберите минимальный пример по теме «Подготовка и настройка среды» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 9. Введение в AWS и Elastic Beanstalk

Liveness отвечает, жив ли процесс; readiness — способен ли экземпляр принимать трафик. Эти проверки не должны выполнять дорогой полный бизнес-сценарий.

Контейнер запускается не от root, получает секреты во время исполнения и содержит только необходимые production-зависимости.

В контексте этого урока раздел связан с задачей: Артефакт сборки и engines

Жёстко записанный секрет попадает в image layers и остаётся там после удаления из последнего слоя.

### Применение

Отделите чистое преобразование по теме «Введение в AWS и Elastic Beanstalk» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 10. Обзор AWS и зачем нужен Elastic Beanstalk

Production-сборка должна быть воспроизводимой: поддерживаемая Node.js, lock-файл, минимальный артефакт и известная команда запуска.

При SIGTERM приложение снимает readiness, перестаёт принимать новые соединения, ждёт активные запросы в пределах deadline и закрывает ресурсы.

В контексте этого урока раздел связан с задачей: Конфигурация окружения и IAM

Deploy без rollback и совместимости миграции превращает обычную ошибку версии в длительный простой.

### Применение

Опишите для темы «Обзор AWS и зачем нужен Elastic Beanstalk» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 11. Обзор AWS

OpenAPI описывает операции, schemas, security и ошибки как машиночитаемый контракт. Swagger UI является представлением контракта, а не заменой самой спецификации.

При SIGTERM приложение снимает readiness, перестаёт принимать новые соединения, ждёт активные запросы в пределах deadline и закрывает ресурсы.

В контексте этого урока раздел связан с задачей: Health checks, logs и alarms

Deploy без rollback и совместимости миграции превращает обычную ошибку версии в длительный простой.

### Применение

Соберите минимальный пример по теме «Обзор AWS» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 12. Зачем нужен Elastic Beanstalk

Liveness отвечает, жив ли процесс; readiness — способен ли экземпляр принимать трафик. Эти проверки не должны выполнять дорогой полный бизнес-сценарий.

При SIGTERM приложение снимает readiness, перестаёт принимать новые соединения, ждёт активные запросы в пределах deadline и закрывает ресурсы.

В контексте этого урока раздел связан с задачей: Blue/green, rolling deployment и rollback

Deploy без rollback и совместимости миграции превращает обычную ошибку версии в длительный простой.

### Применение

Отделите чистое преобразование по теме «Зачем нужен Elastic Beanstalk» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 13. Как работает Elastic Beanstalk

Production-сборка должна быть воспроизводимой: поддерживаемая Node.js, lock-файл, минимальный артефакт и известная команда запуска.

Stateless-экземпляры масштабируются горизонтально. Файлы, сессии и очередь задач выносятся в предназначенные для этого сервисы.

В контексте этого урока раздел связан с задачей: Артефакт сборки и engines

Deploy без rollback и совместимости миграции превращает обычную ошибку версии в длительный простой.

### Применение

Опишите для темы «Как работает Elastic Beanstalk» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 14. Понимание основных понятий и компонентов Elastic Beanstalk

OpenAPI описывает операции, schemas, security и ошибки как машиночитаемый контракт. Swagger UI является представлением контракта, а не заменой самой спецификации.

Stateless-экземпляры масштабируются горизонтально. Файлы, сессии и очередь задач выносятся в предназначенные для этого сервисы.

В контексте этого урока раздел связан с задачей: Конфигурация окружения и IAM

Deploy без rollback и совместимости миграции превращает обычную ошибку версии в длительный простой.

### Применение

Соберите минимальный пример по теме «Понимание основных понятий и компонентов Elastic Beanstalk» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 15. Установка и настройка AWS CLI

Liveness отвечает, жив ли процесс; readiness — способен ли экземпляр принимать трафик. Эти проверки не должны выполнять дорогой полный бизнес-сценарий.

Stateless-экземпляры масштабируются горизонтально. Файлы, сессии и очередь задач выносятся в предназначенные для этого сервисы.

В контексте этого урока раздел связан с задачей: Health checks, logs и alarms

Deploy без rollback и совместимости миграции превращает обычную ошибку версии в длительный простой.

### Применение

Отделите чистое преобразование по теме «Установка и настройка AWS CLI» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 16. Установка Python 3.8

Production-сборка должна быть воспроизводимой: поддерживаемая Node.js, lock-файл, минимальный артефакт и известная команда запуска.

Контейнер запускается не от root, получает секреты во время исполнения и содержит только необходимые production-зависимости.

В контексте этого урока раздел связан с задачей: Blue/green, rolling deployment и rollback

Deploy без rollback и совместимости миграции превращает обычную ошибку версии в длительный простой.

### Применение

Опишите для темы «Установка Python 3.8» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 17. Установка pip

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Артефакт сборки и engines

Отсутствие лимита превращает рост трафика или данных в неконтролируемое потребление памяти.

### Применение

Соберите минимальный пример по теме «Установка pip» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 18. Проверка переменной среды

Конфигурация приходит из окружения, но строки из `process.env` всё равно требуют преобразования и проверки. Ошибка обязательной настройки должна останавливать приложение при старте.

Современный Node.js предоставляет watch mode, env-file, test runner и `util.parseArgs`; небольшому проекту часто не нужны отдельные пакеты для этих задач.

В контексте этого урока раздел связан с задачей: Конфигурация окружения и IAM

Слишком широкий диапазон версий без тестов может незаметно изменить транзитивную зависимость при следующей установке.

### Применение

Отделите чистое преобразование по теме «Проверка переменной среды» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 19. Установка AWS CLI из GitHub репозитория aws-elastic-beanstalk-cli

Production-сборка должна быть воспроизводимой: поддерживаемая Node.js, lock-файл, минимальный артефакт и известная команда запуска.

При SIGTERM приложение снимает readiness, перестаёт принимать новые соединения, ждёт активные запросы в пределах deadline и закрывает ресурсы.

В контексте этого урока раздел связан с задачей: Health checks, logs и alarms

Неверный `trust proxy` позволяет подделать протокол или IP и нарушает secure cookie и rate limiting.

### Применение

Опишите для темы «Установка AWS CLI из GitHub репозитория aws-elastic-beanstalk-cli» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 20. Проверка версии после установки

OpenAPI описывает операции, schemas, security и ошибки как машиночитаемый контракт. Swagger UI является представлением контракта, а не заменой самой спецификации.

При SIGTERM приложение снимает readiness, перестаёт принимать новые соединения, ждёт активные запросы в пределах deadline и закрывает ресурсы.

В контексте этого урока раздел связан с задачей: Blue/green, rolling deployment и rollback

Неверный `trust proxy` позволяет подделать протокол или IP и нарушает secure cookie и rate limiting.

### Применение

Соберите минимальный пример по теме «Проверка версии после установки» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 21. Настройка AWS CLI с использованием aws configure

Liveness отвечает, жив ли процесс; readiness — способен ли экземпляр принимать трафик. Эти проверки не должны выполнять дорогой полный бизнес-сценарий.

При SIGTERM приложение снимает readiness, перестаёт принимать новые соединения, ждёт активные запросы в пределах deadline и закрывает ресурсы.

В контексте этого урока раздел связан с задачей: Артефакт сборки и engines

Неверный `trust proxy` позволяет подделать протокол или IP и нарушает secure cookie и rate limiting.

### Применение

Отделите чистое преобразование по теме «Настройка AWS CLI с использованием aws configure» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 22. Настройка приложения на Elastic Beanstalk

Production-сборка должна быть воспроизводимой: поддерживаемая Node.js, lock-файл, минимальный артефакт и известная команда запуска.

Stateless-экземпляры масштабируются горизонтально. Файлы, сессии и очередь задач выносятся в предназначенные для этого сервисы.

В контексте этого урока раздел связан с задачей: Конфигурация окружения и IAM

Неверный `trust proxy` позволяет подделать протокол или IP и нарушает secure cookie и rate limiting.

### Применение

Опишите для темы «Настройка приложения на Elastic Beanstalk» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 23. Работа с Elastic Beanstalk

OpenAPI описывает операции, schemas, security и ошибки как машиночитаемый контракт. Swagger UI является представлением контракта, а не заменой самой спецификации.

Stateless-экземпляры масштабируются горизонтально. Файлы, сессии и очередь задач выносятся в предназначенные для этого сервисы.

В контексте этого урока раздел связан с задачей: Health checks, logs и alarms

Неверный `trust proxy` позволяет подделать протокол или IP и нарушает secure cookie и rate limiting.

### Применение

Соберите минимальный пример по теме «Работа с Elastic Beanstalk» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 24. Инициализация приложения с помощью EB CLI

Конфигурация приходит из окружения, но строки из `process.env` всё равно требуют преобразования и проверки. Ошибка обязательной настройки должна останавливать приложение при старте.

SemVer помогает оценивать диапазон обновлений, но не доказывает совместимость. Изменение lock-файла проверяют так же внимательно, как изменение исходного кода.

В контексте этого урока раздел связан с задачей: Blue/green, rolling deployment и rollback

Пакет с именем встроенного модуля, например `fs` или `path`, не заменяет API Node.js и обычно вообще не должен устанавливаться.

### Применение

Отделите чистое преобразование по теме «Инициализация приложения с помощью EB CLI» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 25. Создание окружения и развертывание приложения

Production-сборка должна быть воспроизводимой: поддерживаемая Node.js, lock-файл, минимальный артефакт и известная команда запуска.

Контейнер запускается не от root, получает секреты во время исполнения и содержит только необходимые production-зависимости.

В контексте этого урока раздел связан с задачей: Артефакт сборки и engines

Неверный `trust proxy` позволяет подделать протокол или IP и нарушает secure cookie и rate limiting.

### Применение

Опишите для темы «Создание окружения и развертывание приложения» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 26. Изменение кода и повторный деплоинг

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Результат выражается стабильным контрактом, а внутренние ошибки переводятся в безопасную форму без утечки реализации.

В контексте этого урока раздел связан с задачей: Конфигурация окружения и IAM

Обработка только happy path оставляет приложение без определённого поведения при сбое.

### Применение

Соберите минимальный пример по теме «Изменение кода и повторный деплоинг» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 27. Мониторинг и логирование

Наблюдаемость объединяет structured logs, metrics и traces. Request id связывает события одного запроса, но не должен содержать персональные данные.

Несколько процессов используют ядра, но не разделяют heap. Общие сессии, rate limits и jobs выносят во внешнее хранилище.

В контексте этого урока раздел связан с задачей: Health checks, logs и alarms

Логирование всего тела запроса увеличивает стоимость, шум и риск утечки чувствительных данных.

### Применение

Отделите чистое преобразование по теме «Мониторинг и логирование» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 28. Использование EB CLI для мониторинга состояния приложения

package.json описывает контракт запуска: формат модулей, scripts, поддерживаемую Node.js и прямые зависимости. Lock-файл фиксирует полный граф и делает установку воспроизводимой.

dependencies нужны приложению во время работы, devDependencies — для разработки и сборки, peerDependencies описывают совместимость с зависимостью потребителя.

В контексте этого урока раздел связан с задачей: Blue/green, rolling deployment и rollback

Секрет в репозитории остаётся скомпрометированным даже после удаления из последнего коммита; его необходимо отозвать и заменить.

### Применение

Опишите для темы «Использование EB CLI для мониторинга состояния приложения» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 29. Материалы и ресурсы

Сначала формулируется ожидаемое поведение, затем успешный сценарий, известные ошибки и правила завершения. Только после этого выбирается конкретный API.

Входные данные нормализуются на границе, бизнес-правила выполняются отдельно, а побочные эффекты получают явного владельца.

В контексте этого урока раздел связан с задачей: Артефакт сборки и engines

Неявная зависимость усложняет тестирование и приводит к различиям между локальным и production-окружением.

### Применение

Соберите минимальный пример по теме «Материалы и ресурсы» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 30. Официальная документация AWS Elastic Beanstalk

Liveness отвечает, жив ли процесс; readiness — способен ли экземпляр принимать трафик. Эти проверки не должны выполнять дорогой полный бизнес-сценарий.

При SIGTERM приложение снимает readiness, перестаёт принимать новые соединения, ждёт активные запросы в пределах deadline и закрывает ресурсы.

В контексте этого урока раздел связан с задачей: Конфигурация окружения и IAM

Жёстко записанный секрет попадает в image layers и остаётся там после удаления из последнего слоя.

### Применение

Отделите чистое преобразование по теме «Официальная документация AWS Elastic Beanstalk» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## 31. Руководства и примеры кода для Node.js приложений на Elastic Beanstalk

Production-сборка должна быть воспроизводимой: поддерживаемая Node.js, lock-файл, минимальный артефакт и известная команда запуска.

Stateless-экземпляры масштабируются горизонтально. Файлы, сессии и очередь задач выносятся в предназначенные для этого сервисы.

В контексте этого урока раздел связан с задачей: Health checks, logs и alarms

Жёстко записанный секрет попадает в image layers и остаётся там после удаления из последнего слоя.

### Применение

Опишите для темы «Руководства и примеры кода для Node.js приложений на Elastic Beanstalk» допустимый вход, результат и ожидаемую ошибку. Реализуйте happy path, затем проверьте освобождение ресурса при отказе.

**Контрольный вопрос:** Что изменится при запуске нескольких экземпляров приложения?

## 32. Репозиторий AWS Elastic Beanstalk

OpenAPI описывает операции, schemas, security и ошибки как машиночитаемый контракт. Swagger UI является представлением контракта, а не заменой самой спецификации.

Stateless-экземпляры масштабируются горизонтально. Файлы, сессии и очередь задач выносятся в предназначенные для этого сервисы.

В контексте этого урока раздел связан с задачей: Blue/green, rolling deployment и rollback

Жёстко записанный секрет попадает в image layers и остаётся там после удаления из последнего слоя.

### Применение

Соберите минимальный пример по теме «Репозиторий AWS Elastic Beanstalk» на Node.js 24. Добавьте верхний лимит времени или размера и тест превышения этого лимита.

**Контрольный вопрос:** Какая часть решения создаёт backpressure и где должен находиться лимит?

## 33. Список полезных команд EB CLI для быстрой справки

Конфигурация приходит из окружения, но строки из `process.env` всё равно требуют преобразования и проверки. Ошибка обязательной настройки должна останавливать приложение при старте.

SemVer помогает оценивать диапазон обновлений, но не доказывает совместимость. Изменение lock-файла проверяют так же внимательно, как изменение исходного кода.

В контексте этого урока раздел связан с задачей: Артефакт сборки и engines

Секрет в репозитории остаётся скомпрометированным даже после удаления из последнего коммита; его необходимо отозвать и заменить.

### Применение

Отделите чистое преобразование по теме «Список полезных команд EB CLI для быстрой справки» от I/O. Подмените внешнюю границу в тесте и проверьте повтор операции.

**Контрольный вопрос:** Как корректно отменить операцию и кто отвечает за закрытие ресурса?

## Цельный пример урока

Ниже приведён актуальный минимальный пример. Он размещён прямо в материале, чтобы теория и код не расходились по отдельным каталогам.

### `Procfile`

```
web: node server.mjs
```

### `package.json`

```json
{
  "name": "lesson-7-modern-example",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node Procfile",
    "test": "node --test"
  }
}
```

### `server.mjs`

```js
import { createServer } from 'node:http';
let ready = true;
const server = createServer((req, res) => {
  if (req.url === '/health') return res.end(ready ? 'ok' : 'stopping');
  res.end('Node.js on Elastic Beanstalk');
});
server.listen(process.env.PORT ?? 8080);
process.on('SIGTERM', () => { ready = false; server.close(); });
```

## Итоговая работа

Соберите небольшой проект по теме урока. README проекта должен содержать назначение, требования, команды запуска, контракт входа и выхода, перечень ожидаемых ошибок и описание решения для graceful shutdown.

Минимальная проверка:

- успешный сценарий;
- неверный ввод;
- отказ внешней зависимости;
- timeout или отмена;
- повтор операции;
- завершение процесса;
- отсутствие секретов в логах и репозитории.

## Официальные источники

- [AWS Elastic Beanstalk Node.js](https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/create_deploy_nodejs.html)
- [AWS deployment policies](https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/using-features.rolling-version-deploy.html)
- [Документация Node.js 24](https://nodejs.org/docs/latest-v24.x/api/)

- [Поддерживаемые версии Node.js](https://nodejs.org/en/about/previous-releases)
- [Node.js Learn](https://nodejs.org/en/learn)

## Критерии завершения урока

- Вы объясняете механизм без привязки к одному примеру.
- Вы понимаете границы встроенного API и внешней библиотеки.
- Вы умеете обработать ошибку, timeout и отмену.
- Вы можете проверить решение автоматическим тестом.
- Вы знаете, где найти статус и документацию использованного API.
